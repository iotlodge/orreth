# 0070 — The Mirror and the Voice

*Drafted 2026-09-08 from the orreth-EnterpriseRAG proof's charter — the proofs era's sixth
kernel dive: conversation memory, the human's profile finally read, and the persona doctrine
made mechanism. Grounded in the profile/session/voice survey of 2026-09-08 (Appendix A) and
two standing promises this dive collects: 0046's "persona waits for Profiles at zero future
cost" and 0069's language-preference hook.*

***Status: 🟡 DRAFTED · LOCKED — both locks taken by JB's word, 2026-09-08, plainly stated
per the language law: L1 BUILD THE PERSON NOW (the signer registry — people become
identities with their own seat keys, minted at becky's gate; the floor key stays as the
honest anonymous fallback; 0012's debt paid at last) · L2 THE CARVE-OUT APPROVED (persona
assets are purpose — prod-editable through the one gate for ALL residents, kernel nine
included, every edit a versioned sibling; ability prompts stay firmware — JB's charter
word made kernel law).***

## 1. The demand, verbatim (the proof's charter)

> "This is where we get the Profiles working correctly. This means Human needs a profile
> that the Residents and Capabilities understand and leverage (should already be in place
> to a degree)." — addendum 5
>
> The persona doctrine (addendum 6): *"I don't want the brief cryptic replies of Orreth and
> want Humans to feel personalities across the Residents. In general the Universe is helpful
> but not opinionated but remains professional. Residents should have agent profiles
> reflective of the Expertise but want them to be helpful, clear, and honest. Humans should
> be able to update as they desire (version controlled of course)."* — with the razor
> already in JB's word: **persona = prod-changeable purpose (how a voice sounds); role
> ability = firmware (what a voice may do).**
>
> The canvas: full dialog with session/history; the scrub ("what did we believe last
> Tuesday?"); the Memory-Augmented variant (0065) needs conversation worldlines; the
> answer-language preference lives in the profile (0069's hook, landed here).

## 2. The problem — what the survey found

1. **The profile is written, labeled, withdrawable — and read by no one who answers.**
   Sovereignty is real (human assertions enter trusted; inferences must name evidence;
   forgetting is consent-then-shred), the Mirror genuinely assesses conversations into
   observations — but the grounding kit that reaches the model carries **no profile
   slice**. 0025 deferred exactly this ("per-resident personalization adoption") and it
   was never collected.
2. **Conversations do not exist as memory.** An exchange record carries a client-minted
   random session string and **no lineage** — every audience record is an orphan; the
   thread lives in the browser and dies when the pane closes; no reply has ever seen an
   earlier turn (every model call in the repository is single-turn). Meanwhile the
   worldline idiom the fix needs is proven five times over.
3. **The persona layer is a Rust constant.** Every resident wears the identical
   resident-voice prompt (identity, place, a length cap, a grounding rule — no
   disposition, no expertise); the only character text is hardcoded blurbs in the plane's
   source. And the capability residents already shame the kernel's own: charles, charlene,
   and chad carry shelf-borne, version-controlled, ref-cited personas today.
4. **"Last Tuesday" is unparseable and beliefs can't ride the clock.** The time dial
   needs ISO dates, is never called from the worker's answer path, and profile records
   are barred from the projection by the privacy floor. For *beliefs*, versions-as-time
   (replaying a lineage chain to a cutoff) is the truthful primitive — and it exists in
   the knowledge module already.
5. **The kernel does not know which human is speaking.** One seat key per floor: a thumb,
   a preference, a profile is per-*floor*, not per-*person* — the old 0012 signer-registry
   debt, and a wall the proof's enterprise reality (authority-scoped answers, multi-human
   estates) runs straight into.

## 3. The shape

### 3.1 Conversations become worldlines (plain words first)

**In plain words: a conversation becomes a real thread the machine remembers — each turn
linked to the one before it, the whole thread findable later — instead of a picture the
browser repaints and forgets.**

- Audience records gain lineage (`derived_from` to the prior turn — the proven idiom);
  a session becomes a first-class thread with a head; the truncation caps become dials
  with the full turns riding bodies.
- **Replies see the thread**: the voiced lane composes prior turns (recent turns whole,
  older turns as the memory-retriever pattern the scope notes teach — rewrite the ask
  with memory, then retrieve), within a token budget. This is the Memory-Augmented
  variant's substrate, built where every channel shares it.
- The scrub lands honestly twice: **events** scrub by the time dial (grown a small
  deterministic relative-date parser, with the 0066 analyzer's escalation pattern for
  ambiguity); **beliefs** scrub by versions-as-time — replay the claim's lineage to the
  cutoff, which is what "what did we believe Tuesday" truly asks.

### 3.2 The profile is finally read

- A **profile slice** enters the grounding kit through the librarian's sovereign door
  (the privacy floor stands — profile records never enter any projection or corpus;
  they are injected structurally, provenance-labeled: *you told me / I observed / the
  mirror noticed*).
- **Preferences become typed claims**: `{"prefers": {language, verbosity, …}}`,
  human-asserted therefore trusted, correctable by re-assertion, withdrawable by
  consent. The answer-language preference (0069's hook) lands here and the voiced lane
  honors it.
- The Mirror keeps its seat and its hygiene; its observations now *reach* the voice
  instead of only the portrait panel.

### 3.3 The persona layer — 0046's promise, collected under JB's razor word

- **Per-resident Chronicle craft**, worn via a `⟦persona⟧` slot at the one prompt-assembly
  choke point every resident's voice already passes through. The **Universe default
  persona** (helpful, not opinionated, professional) is the base asset every resident
  inherits; each resident's persona is expertise-reflective and stays helpful, clear,
  honest — the doctrine's dials (disposition · register with cryptic out of range ·
  expertise voice · opinion stance · the inherited tighten-only honesty floor) as the
  asset's declared shape.
- **The razor amendment (staged as L2, JB's charter word made kernel law):** persona
  assets are classed **purpose** — prod-editable through the one gate, versioned
  siblings, for *all* residents including the kernel's — while ability prompts (what a
  voice may *do*) remain firmware. The prod razor's carve-out is explicit, never
  implied.
- **The Rust blurbs retire honorably**: served from the shelf with the literal as
  genesis fallback — the most persona-shaped thing in the system stops being a
  constant no gate can reach.

### 3.4 The person behind the seat (staged as L1)

The signer registry the kernel deferred since 0012: **humans as identities** — a person's
own seat key, minted at becky's gate like any identity, so a profile, a preference, a
thumb, and an authority-scoped answer belong to a *person*, not a floor. The floor seat
remains as the anonymous fallback (a walk-up human is still served, still signed, still
honest about being unattributed).

## 4. The razor applied (plain words in parentheses)

| Layer | Class |
|---|---|
| Thread lineage, the profile-slice injection, the signer registry, the relative-date parser (what cannot be quietly edited) | **Firmware** — release |
| Personas (Universe default + per-resident), preference claim shapes (what humans write and tune) | **Craft** — gated, versioned — **classed PURPOSE by the L2 amendment** |
| Thread caps, memory-window sizes, mirror cadence (numbers with limits) | **Dials** |
| Turns, threads, claims, preferences, observations (what was lived) | **Records** — sovereign where the floor says so, purge-reachable |

## 5. The locks (staged for JB, 2026-09-08)

- **L1 — the person behind the seat**: build the per-person signer registry now (0012's
  debt, the proof's enterprise need) versus park it and accept per-floor profiles.
- **L2 — the razor amendment**: persona assets classed as purpose (prod-editable voice)
  for all residents, kernel residents included — confirming JB's charter word as kernel
  law, with the collision named honestly (the razor as written would refuse
  `vera-persona` in prod).

## 6. The spoonfuls (proposed order)

1. **sp1 — The thread.** Lineage on audience records; the session head; replies see
   recent turns; the DOM-only thread retired. Suite: a thread rebuilds from records
   alone; turn N cites turn N−1.
2. **sp2 — The profile read.** The slice in the grounding kit (sovereign door, labeled);
   typed preferences; language honored by the voice. Suite: privacy floor holds (no
   profile in any projection); a withdrawn claim stops speaking mid-thread.
3. **sp3 — The personas.** The Universe default + the first resident personas; the
   `⟦persona⟧` slot; the razor amendment landed; blurbs from the shelf. Proven by a
   human hearing two residents sound like themselves.
4. **sp4 — The scrub.** Relative dates; events by the dial, beliefs by lineage replay;
   proven on "what did we believe last Tuesday" in the glass.
5. **sp5 — The person.** The signer registry (per L1); profiles/preferences/thumbs
   re-homed to persons; the floor seat as honest fallback.

## 7. The honest boundary rows this dive moves

- The profile row: from "written but unread" to "read at every voiced answer, sovereignly."
- New rows: conversation worldlines (rebuild-from-records proven); personas versioned and
  worn; the scrub's two clocks; the signer registry (or its honest park, per L1).
- The 0046 promise and the 0025 deferral: both collected, cited, closed.

## Appendix A — evidence trail (survey 2026-09-08)

Profile sovereign + unread (`profile.py:45-83`, `canon.py:61-63`, the missing slice in
`parlor_facts`/`_listen_kit` `console_worker.py:8333-8484`, 0025's own deferral
`0025:70-83`); the Mirror working but statistical (`mirror.py:50-153`); orphan exchanges +
DOM thread (`parlor.py:1555-1560`, `console_worker.py:9171-9176`, `window.html:4482,4887`);
every model call single-turn (four sites); the worldline idiom five-times proven
(`continuity.py`, `mirror.py:148-153`, `farm.py`); the one choke point
(`console_worker.py:8540-8546`); the razor collision (`console_worker.py:11651-11670`);
Rust blurbs (`main.rs:1234-1330`); capability personas already shipping
(`capabilities/*/genesis.py`); time dial ISO-only + never on the answer path
(`stacks.py:124-138`); versions-as-time (`knowledge.py:140-156`); one seat per floor
(`console_worker.py:289-303`); 0046's promise (`0046:13-14, 41-43`); 0069's hook
(`0069:134-135`).
