# 0051 — The Reins (the lifecycle spine of season 0049)

<!-- PROVENANCE: Fable 5 (claude-fable-5) — designed 2026-08-11, opened on
     JB's word after 0050 closed; carries his approved req-622 (quinn's
     design-change items) as freight. The human has the seat (0030) —
     this dive gives them the reins: see the whole journey, stop anything,
     answer back, and know the mind by its face. -->

**Status: 🟢 OPENED 2026-08-11 — four spoonfuls; locks stage at their
spoonfuls. quinn grades the close.**

## 1. The one-line brief

An Objective's whole life becomes VISIBLE to the human who caused it — and
governable by them at every step. Four limbs: **the Journey** (where is my
ask, in plain words), **Cancel** (covenant rule 11 — the human can always
stop what the machine manages), **the words back** (every gate takes a
reply, not just a click), and **the studio's face** (the mind that reads
your asks becomes a resident you can visit). The judge is unchanged: the
stranger. quinn walks the finished spine and her friction count is the
closing measure.

## 2. The Journey (0049-A1)

The 0047 state ladder — composed → understood → planned → **waiting on
you** → executing → assembling → resolved → assayed — becomes a strip ON
the objective card (no new tab; navigation is thread B's dive):

- each stage in plain words from the SHELF (wave-2 sentences — the glass
  literals quinn wounded become craft): *"read and understood"*, *"a plan
  waits for your word"*, *"working — 3 of 8 pieces home"*…
- the current stage GLOWS; **waiting-on-you says what your word does**
  (req-622: «staged» explained forever);
- times ride each stage (how long it waited, how long it ran);
- floors wear their LEAF names with the full path on hover (req-622: raw
  scope paths off the human's card);
- the monitored set: objectives Orreth still tends (standing charters'
  children included) listed where the human looks, not discovered.

## 3. The Reins — CANCEL (0049-A2, covenant rule 11 at JB's lock)

The draft law (0049 §9-L2, JB locks at sp2): *"The human can always stop
what the machine manages. Every managed objective, standing duty, and
reflex offers a governed cancel/rest; cancellation is a first-class
recorded act, never a deletion; in-flight work stops at the next safe
boundary and says what it left undone."*

The four vectors, mechanically:

| Vector | The rein | Semantics (proposed, locked at sp2) |
|---|---|---|
| Objective (human) | **cancel** on the card, any stage before resolved | staged → cancelled without fanning; executing → undispatched legs never fan, dispatched legs finish and are marked *"completed before your cancel"*; the close-out says what ran, what stopped, what was left undone. Nothing rolls back by itself — reversal is the deed family's craft (0042), offered, never assumed |
| Schedule (charter) | **rest** on the charter's card | active→false as a governed act; already-fired instances keep their own lives (each is an objective with its own rein) |
| Trigger (reflex) | **rest** on the reflex | active→false as a governed act from the glass — the state file's word, finally behind a door (the Tavily incident's hand-edit becomes a lever) |
| A2A (legs) | riding the objective's cancel | a cancelled parent resolves its pending legs *"cancelled — the origin withdrew"*; a floor mid-work finishes and its answer is kept, labeled |

Every cancel lands a **cancellation record** deriving from what it stopped:
who, when, what was left undone. Cancelled is a POSTURE on the worldline,
never an absence (0032's law, generalized).

## 4. The words back (0049-A3)

Every gate card grows a voice line: the human may attach WORDS to any
decision. Approve + words → the words ride the resolution, on the record,
shown on the card forever. **Decline + words → the words route exactly like
a 👎** (0048's loop: the studio reads them, the organs answer) — a declined
plan with a reason becomes fuel, not silence. No new machinery: the thumb's
pipeline, given to the gates.

## 5. The studio's face (0049-C1)

The mind that reads every ask becomes visible (allen's pattern):
- a **calling card** — its charter in plain words ("I read your asks and
  draft their plans"), its recent readings count, its confidence trend;
- a **parlor seat** — ask it *"what did you understand of my request?"*,
  *"why these floors?"* — answers grounded in its own reading records;
- **orrery/brain presence** — the studio's worldline visible like any
  resident's, its thoughts metered under its own DID as they always were.

## 6. Carried freight (req-622 — JB's word, landed here)

1. The lock-4 fallback label reworded PLAIN (and moved to the shelf):
   *"a simple fallback plan — the budget split evenly across the floors,
   because the studio hasn't answered yet; a mind's plan may still replace
   it"* (final words tunable at the gate forever after).
2. «staged» and every status word explained on the card (§2).
3. Raw scope paths off the human's card (§2).
4. **Wave-2 extraction**: the objective card's glass literals become shelf
   sentences (status words, section labels, the report-record line quinn
   flagged) — served through a small `/sentences` door so the glass renders
   from the same shelf the worker speaks from.

## 7. The spoonfuls

1. **sp1 — the Journey**: the strip on the card, wave-2 sentences on the
   shelf, plain status vocabulary, leaf-named floors, the monitored set.
   **LANDED + PROVEN LIVE 2026-08-11.** Fourteen journey sentences on the
   shelf (suite 331→332): the lock-4 label RETIRED by JB's own word — the
   constant deleted, five use sites speaking «plan-fallback-label» from the
   shelf; «journey-declined» extracted parity-true; the rest born plain.
   The worker grew the `/sentences` door (active heads over genesis — the
   glass renders from the SAME shelf the worker speaks from, one voice on
   both faces), and the glass grew `say()`/`journey()`/`leafName()` + the
   monitor line. Proven whole on a real objective (req-624): the staged
   card wore `asked → read and understood → planned →` into the GLOWING
   "waiting on you — approve sends the work to the floors; decline stops it
   here and keeps the record" (req-622's «staged» demand, answered
   forever); floors leaf-named (`naive · rag`, raw paths on hover only);
   "Orreth is tending 1 objective(s) — 1 waiting on you · 0 working" above
   the ledger; approved → resolved → the strip closed `done — the report
   below is yours` with the observatory's promise trailing and the report
   line human. Polish noted for sp4's grade: a card resolved before its
   reading lands still says "being read" — the honest late-reading label
   is quinn's to demand.
2. **sp2 — the Reins**: cancel/rest across the four vectors + the
   cancellation record; **covenant rule 11 lands at JB's lock** (the
   covenant file changes only with his word — the lock IS the word).
3. **sp3 — the words back**: the voice line on gate cards; decline-with-
   words routes through 0048's loop.
4. **sp4 — the studio's face + the grade**: card, parlor seat, rail
   presence — then **quinn re-walks the same objective family and the
   friction counts are compared, before vs after**: the dive's closing
   number is hers, not ours.

## 8. Convergence

| Organ | What it supplies |
|---|---|
| 0047 | the state ladder the Journey renders; the studio the face embodies |
| 0048 | the loop that receives declined-with-words; quinn's channel |
| 0050 | the shelf the Journey's words live on; the door that tunes them |
| 0042 | reversal-as-craft — cancel offers the deed family, never assumes |
| 0032 | cancelled-as-posture on the worldline, never absence |
| 0020/0028 | the parlor seat and workspace the studio's face wears |
