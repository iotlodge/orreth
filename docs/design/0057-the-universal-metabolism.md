# 0057 — The Universal Metabolism (every floor breathes)

<!-- PROVENANCE: Fable 5 (claude-fable-5) — drafted 2026-08-17 at JB's
     word ("open the metabolism dive"), as the memory program's next step
     and the named cure for the growing-corpus disease (the performance
     standing law, 2026-08-16). Tabled at the open, JB's word: a human
     CRUD surface for MACHINE-level tasks — parked for the UI
     continuity/usability season, seed on record. -->

**Status: OPEN — sp1 LANDED 2026-08-17 (proven live: 7 distinct floors
breathed — u:demo 6.5k-records ground ≤2.7s/breath, f:prod, e:cloud,
e:retail, e:care, and f:charles answering the honest "nothing due" of a
five-day-old floor; reports with measured loss on each floor's record;
dials planted per floor on first touch; the Brain door tells the
round-robin truth). BONUS FIND fixed en route: the wire beat had NEVER
landed a report — `_N` lacked `parent`/`children`, so the loss walker
crashed after every real distillation since wire-honesty week (rag: 9
distillations, 0 reports); "every forgetting is measured" was silently
false on the wire until today. sp2 next.**

## 1. The disease and the cure

The brain holds **19,177 records** across 23 floors and grows every hour
the desks walk. The performance law (JB, 2026-08-16: "please please
please") says sub-second rooms are a standing requirement — and the
read law + memos are treatment, not cure. The cure was named in 0039 and
built in wire-honesty week: **the metabolism** — records whose class
window passes without recall distill into compact knowledge, the
recalled stay warm, every forgetting lands a report with its measured
loss in bits.

The dishonesty this dive ends: **the metabolism breathes on ONE floor**
(`e:rag/f:naive`, ~300 records) while f:prod holds 6,395, e:retail
2,992, the universe floor 6,536 — the sickest organs are the ones the
cure never visits.

## 2. The laws inherited (nothing new here — that is the point)

- **One law, both grounds** — `canon.metabolism_beat` is the only
  metabolism; the wire runs the same function the sim proves.
- **Distillation never deletes** — the log is append-only (0002); a
  distillation is a NEW record with `derived_from` lineage. The win is a
  bounded working set for projections, never a shrunken chronicle.
- **Usage is evidence** (locked 2026-07-23) — recalled records stay warm
  and low. The tap is persistent per scope (`stacks/<scope>/recalls.json`).
- **The dials are data** — per-class windows as a Canon asset
  (`distillation-dials`), human- and gate-editable, never code.
- **Every forgetting is measured** (0033) — reconstruction entropy on
  the record, tuning by evidence.

## 3. The spoonfuls

**sp1 — the beat walks every floor.** `metabolism_wire_beat` trades its
hard-coded rag floor for a round-robin over `FLOOR_SCOPES`: each tick
(default 900s) metabolizes ONE floor, so a full cycle crosses the world
in ~6 hours and the cost per tick stays a single floor's projection.
Dials plant on first touch per floor (same v1 windows). Reports land on
each floor's own record. The projection stays tags-first (the read law).
Proof: metabolism-reports on ≥3 floors that never had one; the worker's
log names each floor as it breathes; cold rooms stay ≤1s.

**sp2 — usage is evidence everywhere.** Warmth today flows only from
the ask lane. The reads that mean a human or resident USED a memory now
tap: the desk door's report reads and the ask lane's hits. Census,
telemetry, and the metabolism's own sweeps never warm a record — a
machine reading itself is not usage. Proof: open a desk report, watch
its id stay warm past its window on the next cycle.

**sp3 — the glass tells it.** The Brain pull's `distilled` gauge stops
reading 0: distilled counts per partition, the freshest loss figure, and
the last-breath age per floor — telemetry, never paraphrase (JB's law).
Proof: the 🧠 pull shows the metabolism moving on floors it never
touched before, and the numbers agree with the reports on the record.

## 4. Named defaults (JB may veto any; none block sp1)

1. **All discovered floors join** — including u:demo itself and the
   capability floors. Purpose records are protected by the same physics
   as everything else: nothing deletes, and the desk's reports index
   reads the log, not the working set.
2. **Untapped floors distill by age alone** at first — safe because
   sp2 lands warmth before any second cycle, and the windows are
   conservative (observations 30d, knowledge 90d, intentions 90d).
3. **The beat stays ungated** — it has run ungated on rag since
   wire-honesty week with every report on the record; extending the
   same law to more ground is not a new consequence class.

## 5. Tabled at the open (JB's word)

A human CRUD surface for machine-level tasks (this beat, the yardstick,
the sweeps — the no-resident identities). Parked for the UI
continuity/usability season; seed: `machine-task-crud-seed` in memory.
