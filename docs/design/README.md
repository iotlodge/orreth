# Design — the build-phase specs

The vision (`../vision/FUTURE-the-orreth.md`) is the *what* and the *why*. This folder is the *how* —
turned into buildable specs, one keystone at a time. Each doc is design-phase: schemas, contracts,
rationale, and open decisions. **No implementation code lives here** — that arrives only after a spec is blessed.

**Rebaselined 2026-06-30 — memory-first.** The center is the *memory substrate for living identities*; governance is its
first application. The dives below are re-ordered around that center. The 6 use-case-derived requirements
(`../decisions/README.md` → Open) are hard inputs to the substrate keystone.

> **Design phase complete — 2026-07-02.** All fourteen dives (0000–0013) are drafted, every decision in the
> ledger is locked, and each close-out landed as contract + enforced simulator code (36/36 tests). The build
> phase begins per 0000's blessed sequence: the Rust plane (`orrethd`) against the conformance suite, the pane
> (0008), and the League (PG-1). **Queued from the build-phase dialogs:** `0014 — the Knowledge
> Loop` (Knowledge Categories versioned by universe-time · Orreth.agent the librarian · external
> sources as identities · frozen Datasets · propagation with recall + generation depth — see
> `../vision/the-knowledge-loop.md`) and `0015 — Orreth.agent, the chassis` (one fixed
> GraphSpec-authored loop — prepare · plan · parallel nucleus · reflect/critic · replan ·
> breaker — behavior as a profile: Policy, Prompts, Skills, Persona; the parking lot that turns
> failure into knowledge-acquisition intents; Master+swarm as teacher-auditor with a measured
> gap — see `../vision/orreth-agent-the-chassis.md`).

## The build order (set 2026-07-14 — JB delegated sequencing to the design owner)

> **A — the science gets legs:** 0033 sp1 (infotheory harness) → sp2 (distortion contracts)
> → sp3 (the coordinate, soft — tags only). No locks needed; starts on JB's word.
> **B — the desk opens:** 0032 sp1–4 (deliveries born citing coordinates; delivery-value = 0033's
> information gain — the first organ consuming the physics).
> **C — the playing begins:** 0034 sp1 (continuity template + label canon — *the second brain
> stands*) → sp2 (consent & delegation; survivorship still held) → sp3 (the Mirror) → sp4 (the
> Brain Glass, wearing the harness numbers).
> **D — the one rule-9 gate, everything together:** the coordinate's hard landing into
> contracts/v0 + **0031 sp2 (the aperture)** + review_interval dial + β-per-tier — one JB
> session, per-field approval, against months of soft-landed evidence.
> **E — the last key element:** 0022 Phase 2 (the meaning axis) — built last so the hybrid
> ranker consumes coordinates and aperture scores instead of being rebuilt for them.
> **Alongside, never blocking:** the article campaign (`../articles/visibility-campaign.md`).
> **Parked with named gates:** survivorship (own dive — **drafted as 0035**, staged for JB's
> deliberate lock) · clinical universe (needs a partner) ·
> failing-runs lens (needs Phase 2) · glass warts (fold into Brain Glass).

## The road from 0041 (set 2026-07-25, after the Epoch closed whole at 1c6b423)

Written down so any session — any model — can pick this up cold. Order is
leverage-first; each step names its gate.

> **1 — the rig wears its era — DONE 2026-07-26** (rebuilt; the glass serves the
> 0.42 whisper, the pgate, and the honest lag-card label).
> **2 — the Epoch earns its harness — DONE 2026-07-26 except one gap:**
> `demo_drift.py` shipped 07-25 (`demo.sh drift`) · the machinery lifted into
> `orreth_sim/epoch.py` with conformance tests (suite 244→254, THE SILENCE held) ·
> the 0038 orphan-lineage fix landed (the card carries `proposal_ref`, the adoption
> names it). **REMAINING: the universe-scope lag card gap** (a universe-level oath
> that will not converge has no port to stage at).
> **3 — the campaign sees the new machine — DONE 2026-07-26:** demo.orreth.ai
> serves the v0.42 moment; the deploy prune of /deeds/ + /media/ is RETIRED
> (exclude in the stack, proven on the deploy itself).
> **4 — the next dive: 0042 The Deed** (reserved above, triaged + locked by JB
> 2026-07-25): draft → JB's locks → spoonfuls. The effect protocol — effect classes
> as Canon assets, ceremony priced by class, the record family
> intent → authorization → attempt → receipt → independent observation →
> reconciliation → closure. First proof: allen's apply formalized (0037 already
> walks the shape); second class: real outbound publishing.
> **Alongside, never blocking:** allen's standing TODOs (AWS-docs supply line ·
> walk receipts on his floor · DAG polish · Daylight Glass · the rig's provisioning
> lifecycle as his proving ground) · ~~librarian card/tile readability wart~~
> (FIXED 2026-07-26 — a 0037-era flex regression blockified the stat tiles;
> panel bodies now wrap and scroll) · ~~Daylight Glass~~ (BUILT 2026-07-26 —
> one glass, two lights: ☀/☾ in the header; the day brain is ink-wash on
> paper via `multiply`, the day orrery a hand-drawn chart; every canvas color
> in one THEMES object, night pixel-faithful) ·
> guides 03 (Resident Field Guide) + 04 (Building Agents/SDK).
> **The standing register:** [the-honest-boundary.md](the-honest-boundary.md) —
> claims with evidence named, partials, and parks in one place (seeded
> 2026-07-26 from the outside poster's §12). A dive that moves a row updates
> the register in its closing commit.
> **5 — 0043 The Observatory: CLOSED WHOLE 2026-07-31, era 0.43** (designed
> + 4 locks + five spoonfuls in two days): vera the astronomer, the flight
> recorder on the plane/farm gateways, the two-tier telemetry law,
> glance·watch·assay, A/B-arms-as-epochs, the instrument glass — seven rows
> first, the whole rig delivered.
> **sp1 EATEN 2026-07-30**: the flight recorder — four taps (plane · farm ·
> fingertip spans · gate-wait ages), the Series projection with declared
> retention and measured distillation loss, the wire twin in the worker
> (`FlightBook`, ledger-seeded, aged honestly); proven live — the first
> voiced reply put a real refusal AND its thought on the book.
> **sp2 EATEN 2026-07-30**: vera & the assay loop — the tenth organ at the
> universe floor (the improver's precedent), all four laws structural
> (another floor's mind signs · refusal over self-grading · cards never
> levers · her cost metered under her own DID, PINNED so a squeezed budget
> halts loudly), the glance·watch·assay dial (default glance — depth costs
> money and says so), human gradings on the same shelf; proven live on the
> rig. Rider closed: the universe-scope lag card was already wired
> (verified) — the register row retired.
> **sp3 EATEN 2026-07-30**: the instrument room — the 🔭 Observatory tab in
> the Console: six hand-rolled panels supplied by ONE payload from the
> worker's own door (:4562, plane untouched), every panel wearing its tier
> chip, the recorder's pulse first-class (a dark door turns the room dark
> LOUDLY), both lights proven in Chrome; first render surfaced a purge
> 19 days waiting at its gate. **The gap register (§11) is now standing
> law**: found → written → homed — G1/G3/G4/G5/G9 closed this spoonful.
> **sp4 EATEN 2026-07-30 — THE CROWN JEWEL PROVEN LIVE, WHOLE**: A/B where
> each arm IS a cryptographically named machine and the rollout is a signed
> constitutional act — routing-ab (router vs hybrid) ran the full loop on
> the rig: proposed → split opened on a click → real asks wore their arms →
> sonnet-5 judged under vera's declared ceiling → concluded at min-n → the
> promotion card adopted on a click, `adopted_from` + `derived_from` naming
> the variant AND the experiment, the loser on the shelf. (The rollout was
> then honestly ACCUSED of drift — the watchdog's vocabulary lacked the new
> word; JB left the accusation on record and «experiment» joined
> ADOPTION_KINDS with a conformance story.) The dial turned glance→assay by
> governed act (G4); the ceiling enforced (G5); wire rubrics landed (G6);
> G10 found and homed.
> **sp5 EATEN 2026-07-31 — THE DIVE CLOSES WHOLE, era → 0.43**: the
> examiner round-robins all 19 floors with the ceiling closing mid-round;
> the rollup in the room's header beside the Observatory's own price; the
> pyramid survives the process (G2 — dump/load sim-held, summaries + book
> compaction live); the bench chosen (G7); wire flow spans on signed
> records; and the dial's word outlives the process (G10 — turned, worker
> restarted, the word held; turned back the same way). Remaining honestly
> parked: daemon-side dead-man + out-of-band alerting (one future dive —
> they share a channel), G6's first rubric'd objective. 283/283.
> **Parked, awaiting JB's stamp:** 0040 The Faculty & the Agent Lab (built only when
> Orreth is "stamped ready to test") · the Mandate discussion (objective homeostasis
> + attention budget).

## The dive sequence

Ordered by leverage — each unlocks the next.

| # | Spec | Why it's a keystone | Status |
|---|---|---|---|
| **0000** | [Structure & Interoperability — the frame](0000-structure-and-interoperability.md) | One node (`orrethd`), tier = profile, layered deployment; contracts as the product; Rust/Python/TS split ("lift the contract, port the engine"); Field is native Orreth (CO = reference proof, optional adapter); dev compose topology; discharges the 2026-07-01 review findings; proposes re-sequencing + reserved dives 0010–0012 (AgentField/Gateways · Factories · HITL). | 🟡 draft for review |
| **0001** | [Promoted Memory & Skill Standard + acceptance rubric](0001-promoted-memory-and-skill-standard.md) | The `MemoryRecord` atom + skills as promoted memories + the rubric. **Reframed:** memory is the substrate; skills/governance are what it powers. | 🟡 draft for review |
| **0002** | [**Living Identity + Retrieval — the substrate keystone**](0002-living-identity-and-retrieval.md) | Identity as universe-unique key; **memory keyed to identity**; the space×time **retrieval contract** (time-budget escalation, Sourced + Verified). Discharges all 6 requirements: **archetype→incarnation**, **memory portability scope**, **cross-tenant retrieval authz**, **showcase/portfolio scope**, **retention/consent**, **Sourced+Verified audit**. **All §8 decisions locked 2026-07-01**; amendments **landed** 2026-07-01: ScopePath-relative addressing, merge semantics, budget-miss ≡ authz-miss, interview query budget. | 🟡 draft for review |
| **0003** | [Pruning Policy — the metabolism](0003-pruning-policy.md) | What each layer keeps vs distills vs tombstones; how "years" stays affordable. **Locked in:** hybrid floors+steward, apex = distilled + signed pointers, tombstones annotate. Distillation record makes provenance survive compression. | 🟡 draft for review |
| **0004** | [Tier Profile & the Two Clocks](0004-tier-profile-and-the-two-clocks.md) | The dials that make a tier — refines the `contracts/v0` head start. **Centerpiece: the two-clock model** (surfaced by The League): *meaning runs on universe-time; security and money run on wall-clock* — declared, monotonic universe-time with high-water enforcement (memory-backdating becomes structurally detectable) + **lived vs ingested-archive** provenance. Adds the owed **objective vector**, retention as **min *and* max** per record class (SOX keep-at-least vs GDPR keep-at-most; legal hold = governed `min: ∞`), profiles as tighten-only cascade artifacts, and the three proving-ground profiles as worked examples. **All §7 decisions locked 2026-07-02**; schema deltas landed (UniverseTime/WallClock, occurred_at/received_at, provenance_class, TierProfile clock/objective/retention_classes); simulator enforces the high-water rule — 12/12 tests. | 🟡 draft for review |
| **0005** | [Run Record & the Monoidal Roll-up](0005-run-record-and-monoidal-rollup.md) | The aggregatable envelope + the monoid that composes it up the tree — standings, quarter-closes, AgentFacts, and benchmarks are one mechanism; raw runs never travel, signed pointers do. **Confidence = Bayesian posterior** (mean + 95% CI + n; per-tier prior applied once at the report edge) — discharges open #7. **Floors flag, never average away** (a 0.98 agent with one breach shows both truths). Portfolio shows **mean + interval + n**. Buckets are universe-time (0004). Contract + simulator landed; **all decisions locked 2026-07-02**; 17/17 tests. | 🟡 draft for review |
| **0006** | [becky — identity & capability chain](0006-becky-identity-and-capability-chain.md) | DID delegation root→leaf (**locked:** did:web roots + did:key leaves); resident (TCB) vs registered (workforce); archetype→incarnation issuance; attenuation-only CapabilityTokens; revocation kill-switch; AgentFacts. **Pulled forward** — 0002's authz assumes it. | 🟡 draft for review |
| **0007** | [The Cascade Resolver](0007-cascade-resolver.md) | One fold, every law: floors tighten-only · soft most-specific-wins (attributed) · skills additive · monotone dials — merge laws travel with the field's contract, never the resolver. **ResolvedContext is content-addressed** (same chain ⇒ same id): policy becomes cacheable, diffable, canary-able. **RunRecords pin their `context_hash`** ("what rules governed this agent" is a lookup) · **partition fails closed + signals** (last-known law persists; staleness is loud; a blip never freezes a season). Contract + simulator landed; **all decisions locked 2026-07-02**; 32/32 tests. | 🟡 draft for review |
| **0008** | [The pane & graph engineering (interop/UX)](0008-pane-and-graph-engineering.md) | The Field commander pane (v1 hero, six regions) + dual-mode authoring (English ↔ canvas, both projections of **GraphSpec v0**, sentence↔node bijection, never-guess-silently) + lane-routed change flow. Recursive pane = same components, ScopePath-parametrized. **Spacetime window concept art shipped** (`../vision/Orreth-spacetime-window-concept`). | 🟡 draft for review |
| **0009** | [Build My First Universe](0009-build-my-first-universe.md) | The closing brace: a **template is a universe as data** (a resolver chain + profiles with a clock + a StampOrder roster + floors + tone), and the provisioner renders it — trust-tier-gated at the door, anonymous caps clamped. **Templates: League · Second Brain · Company** (#12) · **blank universes default REAL; wild is a visible choice; the dial modulates rigor, never floors** (#13, tested: identical floor sets) · **fuel + hibernation + BYO-key** (#14: one-time allotment; out of fuel ⇒ hibernate-never-delete — "it dreams only when fueled"; bounded spend per free user). Contract + simulator landed; **all decisions locked 2026-07-02**; 36/36 tests. | 🟡 draft for review |
| **0010** | [AgentField & Gateways](0010-agentfield-and-gateways.md) | The medium and the doors: one way in, one way out, one way to a model. Workforce joins at the Gateway (leased DID + budgeted token) and holds only the **AgentSurface** (five verbs, SDK-neutral — GraphSpec compiles onto it). **Model Gateway: degrade-where-pins-allow** (honest flags; pinned tiers fail rather than silently dumb down). **Signals are transport unless state-changing** ("if it's not memory, it didn't happen") — the `signal_capture` dial is wild-vs-REAL's first concrete member. vigil taps the gateways: shape, never content. Contract + simulator landed; **all decisions locked 2026-07-02**; 20/20 tests. | 🟡 draft for review |
| **0011** | [Factories](0011-factories.md) | Archetype → incarnation at scale, governed — CO's farms generalized. StampOrder + **BirthCertificate** (identity ops are memory; `born_at` in universe-time; the generation is a roll-up cohort — draft classes are comparable standings-style). **Upgrade in place** (memory survives; re-stamp = a sibling, never a silent successor) · **rookie probation** (judge 1.0 until the first bundle earns 1-in-N) · `stamp_quota` = 0013's trust-tier scale caps made mechanical. Contract + simulator landed; **all decisions locked 2026-07-02**; 23/23 tests. | 🟡 draft for review |
| **0012** | [HITL: Gates, Queues & the Co-sign](0012-hitl-gates-queues-and-cosign.md) | Consequence waits for humans: Escalations (staged by machine — vigil stages, never enforces), GatePolicies (**co-sign bars cascade tighten-only, like floors**), distinct-signer quorum, and **cooling-off as approved-but-held** — three to destroy a universe, one to save it. **Silence never approves** (expiry = deny + vigil signal) · **bars absolute** (no solo-org clamping — "no single employee is a god" from day one; staffing is a published launch gate) · no break-glass, deliberately. Contract + simulator landed; **all decisions locked 2026-07-02**; 27/27 tests. | 🟡 draft for review |
| **0013** | [The Custodian Tier & Responsible-Universe Architecture](0013-custodian-tier-and-responsible-universes.md) | The dual-use reckoning, made structural. Custodian = apex-above-apex (freeze-not-read) · platform floors (meta-cascade, asymmetric change bars) · **vigil**, the Warden resident organ (**detection, not enforcement**; dead-man heartbeat as floor term) · graded quarantine w/ **HITL placement map** · key custody as the fork · governed telemetry (published contract + tenant mirror) · trust-tiered onboarding · abuse taxonomy · **proving grounds** (The League = time axis, playable · Earth Mapper = space axis, dogfooded first tenant · Agentic Enterprise northstar in parallel; funnel **Watch → Play → Build → Buy**). **Provenance corrected 2026-07-02:** both 2026-07-01 passes were Opus 4.8 (the second mislabeled itself "Fable 5" after a silent model fallback — caught via the signed session audit log, the doc's own attribution thesis in action); genuinely reviewed & amended by **Fable 5** 2026-07-02. All §12 decisions JB-locked. Amends `0000`. | 🟡 draft for review |
| **0014** | [The Knowledge Loop](0014-the-knowledge-loop.md) | Learning from the world, governed: Knowledge Categories versioned by universe-time · **external sources as identities** (the source registry) · admitted quarantined at 0.0000 · promotion earned with receipts · **recall by lineage** (`derived_from` walk). | ✅ live |
| **0015** | [Orreth.agent — the chassis](0015-orreth-agent-the-chassis.md) | One fixed GraphSpec loop (prepare · plan · parallel nucleus · reflect · replan · breaker); behavior as a profile; the parking lot turns failure into knowledge-acquisition intents. | ✅ live |
| **0016** | [The Model Plane](0016-the-model-plane.md) | LiteLLM through the floors: config down · calls local · misses up · usage up. Model lifecycle (candidate→…→sunset) — the trust ladder's third application; plane-held budgets; end-to-end metering. | ✅ live |
| **0017** | [The Lifeforce Agents](0017-the-lifeforce-agents.md) | Three agent flavors (prototype · LangGraph · AgentField sentinel) + the orreth-agent SDK; persistent identities that re-join as the same self; becky's join door. | ✅ live |
| **0018** | [The Tool Farm](0018-the-tool-farm.md) | Services are identities with worldlines: DID'd tools/MCPs in a floor-governed farm · manifest hash pin (the rug-pull door, CVE-2025-54136) · lifecycle = the trust ladder's **fourth** application (proposed→probation→serving⇄dropped·quarantined·decommissioned) · drop/rejoin as lease expiry + re-attestation (the SPIFFE lesson) · every transition a keeper-signed MemoryRecord in the spacetime window · decom-with-discredit hands 0014 §4 the recall. | ✅ live |
| **0019** | [The Stable](0019-the-stable.md) | Minds are identities with worldlines — the model plane's missing half: stalls pin the DEAL (pricing/context bytes), price drift walks the rug-pull door, EOL is an appointment on the pasture calendar, and the **universal meter** rides the beat — per-agent tokens and dollars, residents included, honest zeros shown. | ✅ live |
| **0020** | [The Parlor](0020-the-parlor.md) | The interoperability law: **agents, authorized, see data; humans must ask.** The audience room — click a resident, be received; the resident fetches with its own authority and answers on the record. Calling cards make agent UX data, not code (new residents bring new flows, zero core changes); exchanges land resident-signed in the window; grounded always, voiced through one governed metered thought when fueled. Zero plane changes — the decoupling proof. | ✅ live |
| **0021** | [The Shipyard](0021-the-shipyard.md) | **A universe that grows by conversation** (0009's provisioner, first wire landing): ask becky for an ecosystem, answer her fields question, LAUNCH at the gate — real containers on the rig network, each floor beating into the orrery by itself. Plus **one glass, every floor**: ports ride the beat, CORS opens the rig, worlds are doors, breadcrumbs climb back — and the orrery holds its breath while you look. | ✅ live |
| **0022** | [The Memory Construct](0022-the-memory-construct.md) | The brain under the universe (from the Universe-Brain session, `../vision/the-universe-brain.md`): **the signed log is the truth; every index is a rebuildable projection** · Shape A — one Postgres per node (pgvector + BM25 + CTE lineage, hybrid weighted-RRF in SQL, **trust-weighted rerank: `recalled` ranks dead**) + object store + embedded DuckDB/fastembed/Iroh — one container + one bucket per federated node · crypto-shred purge w/ mandatory projection eviction · bi-temporal close-the-window semantics · discharges the pgvector pre-stage (the 0002-amendment). **§8 Phase 1 BUILT + live-proven 2026-07-10**: runs/requests persistence, six indexes, presence epoch-memo, meter rotation (totals + archive), orphan sweep (rule-9 approved) · **Phase 2 the MEANING AXIS, sim-first 2026-07-17 (Phase E)**: local fastembed under the bytes-local lock, hybrid weighted-RRF, standing-over-relevance (`recalled` ranks dead), coordinate/aperture pulls, cross-source contradiction v1, the Mirror hears meaning (proven live); the wire's `Query.meaning` cut awaits the rule-9 gate. | ✅ blessed · Ph1 live · Ph2 sim-first |
| **0023** | [The Librarian](0023-the-librarian.md) | One mind, many seats, no levers (the Universe-Brain session, 0020 §4's reserved seat incarnated): librarian root + per-seat did:key, lineage proves sameness · all-knowledge/zero-levers token (no govern/transfer/issue — ever) · **the self-dialog**: `librarian-ask` across seats, non-refusable intra-universe, `derived-from-opted-out` synthesis evaporates (leak-by-synthesis killed) · the `exchange` TierProfile block (per-class opt-in/out, future-flow only; ops/security never) · query planner deterministic→meaning→cross-seat→gather, citations mandatory · vigil watches her every read. **§7 all four phases landed 2026-07-10** (seats · self-dialog w/ lineage-death · exchange reference + schema · citations); Console toggles + wire steward on the build ledger. | ✅ blessed · live |
| **0024** | [Markers & the Severity Lanes](0024-markers-and-the-severity-lanes.md) | Grading 100% of change (Universe-Brain §6, R5/R6 pre-locked): a marker is a **signed annotation record deriving from what it marks** — never a mutation — carrying `change_severity` (low/med/high/critical) × `life_event` (minor/major/substantial), always a reason, rubrics as cascaded Standards · **the R6 lanes**: low auto-approves, medium co-review+notify, high/critical human+quorum — no existing gate loosened; the auto lane's first resident is the human's own **"remember this"** (quoted, weighted, derived from its audience) · recall walks now grade themselves high. | ✅ blessed · live |
| **0025** | [The Human Profile](0025-the-human-profile.md) | The second brain's heart (Universe-Brain §5, R4 pre-locked): one living portrait per creator, class personal-data — **human assertions enter `trusted` (sovereign), Librarian inferences enter `untrusted` with evidence named** (rookie probation for beliefs about you) · v1 inference: major/substantial remember-markers become observations · reads label provenance ("you told me" vs "I observed") with receipts · **`forget about me:` = consent withdrawal via lineage-death today, crypto-shred at the Purge** — THAT you forgot stays on the record, WHAT you forgot stops speaking · all through the Librarian, never around her. | ✅ blessed · live |
| **0043** | [The Observatory](0043-the-observatory.md) | The universe learns to measure what it actually does (JB's seed 2026-07-30: the missing sense — telemetry/observability/evaluation for residents AND workforce, DeepEval/RAGAS/LangSmith reviewed and deliberately NOT followed): **observability as a projection over the signed log — never a second truth, never a federated sidecar** (the O·I·O·T ladder IS the trace, already signed) · **vera, the astronomer** — the tenth organ (for Vera Rubin), content-AWARE assay as vigil's deliberate inverse; judges always another floor's minds; detection wears no levers; her own cost is her first exhibit · **the two-tier storage law**: assay verdicts/standings/experiments = signed Chronicle records feeding 0039's loop; instrument series = a distilling projection with declared retention — *even the monitoring ages honestly* · **the taps are the choke points we already govern**: the model-plane gateway (every thought metered → flight recorder), charlotte's farm gateway (every tool call), fingertip spans, gate-wait ages · **the dial**: glance · watch · assay (depth costs money and says so) · **the experiment**: A/B where each arm IS an Epoch — a cryptographically named machine — and the rollout is a promotion card at a human gate · the instrument glass in both lights, the RAG observatory panel first. **ALL FOUR LOCKS LANDED 2026-07-30** (vera · two-tier · the name · rows-first and THE WHOLE RIG NOT OPTIONAL — sp5 committed). | 🔭 **CLOSED WHOLE 2026-07-31, era 0.43** — five spoonfuls in two days (283/283): the senses · the judgment · the seeing · the argument · the universe examining itself whole |
| **0044** | [The Bell](0044-the-bell.md) | The last mile of visibility (G1's remainder + G8, committed by JB 2026-07-31): **the witness** — a daemon-side dead-man that notices the worker's silence and says so in a signed record, glass or no glass (author ≠ subject, even for obituaries) · **the bell** — the one organ allowed to reach a human who is NOT at the Console: a farm service with its own DID and pinned transport manifest, no send without standing consent in becky's ledger (0034), content-minimal (scope · kind · age · pointer — never bodies), every ring on the record BEFORE the wire, cooldown against alarm fatigue, and a ring never moves a gate's clock (0012 holds) · founding subscribers: witness · gate-age · **tamper (0042's standing verify beat, folded in by JB's lock)** · **LOCKED 2026-08-02**: SES email v1 · verify-beat in · 90s threshold | 🔔 **CLOSED WHOLE 2026-08-02, era 0.44** — designed, locked, and proven IN ONE DAY: the witness (kill-tested, Console closed) · the bell service (the first ring in JB's real inbox, one-faced refusals, record-before-wire) · the subscribers (a real tamper on the open web caught and rung; the rig's 22-day-old gate rung once) · the whole (two deaths, one email — law 6 the closing proof) |
| **0045** | [The Craft Room](0045-the-craft-room.md) | JB's machine-of-machines brief (2026-08-03): Orreth is 100% agentic and **the machine shows its work but hides its craft** — the driving NLP (prompts · skills · policies · rubrics · charters) invisible to any human who isn't the builder; HITL trust demands the craft be readable · seven laws: registry-as-projection (rule 7) · **two lifecycles, one law of change — Canon change = EPOCH RELEASE (0041's ceremony is the firmware lifecycle), Chronicle rides grace's gates** · the human's edit wears the same one-motion sibling grammar · the argument first-class (0043 experiments from any object; Blue/Green = two named machines, gated cutover) · commissioned creation (create → librarian gathers → factory builds → the bell rings → v1 in the registry) · scale a constraint · **«Governance» replaces the Ask tab** | 🛠 **DESIGNED 2026-08-03, blessing pending** — five spoonfuls queued: the registry & room · the editors & one law · the Canon release · the applied graph & the argument · the commission |
| **0042** | [The Deed](0042-the-deed.md) | The effect protocol — "the actor is not the sole witness of its own success," our own yardstick-law extended into the world (seeded by the 2026-07-25 outside review, loop 2; triaged + locked by JB same day): **effect classes as Canon assets** declaring reversibility, idempotency key, independent observer, blast radius, and **ceremony priced by class** (the universe must not drown in receipts for trivialities) · the record family — intent → authorization → attempt → external receipt → **independent observation** → reconciliation → closure/compensation · first proof: allen's apply formalized into the family (0037 already walks it), second class: real outbound publishing · honest boundary stated: on one laptop, observation is *separated*, not *independent*. Designed, locked (4 JB locks, all recommended), and built sp1–sp4 in ONE AFTERNOON 2026-07-25 (7ac613f · 451eb62 · 50d5955 · the closing commit): the shelf and the family sim-proven — 12 conformance tests, a same-second twin-intent collision caught by sp2's own test before it ever ran live · allen swears his deeds (the estate apply walks the family, the pinned plan's hash the one key) · **the first real deed on the open web** — `/deeds/first-deed.json` published on JB's word, fetched back by the librarian's seat, hashes agreeing — then SWAPPED by an outside hand, CAUGHT by the verify walk, WALKED BACK at the gate on a fresh human word, and published anew through the family. The law ran its full loop on the public internet. Era 0.42. | ✅ blessed · live |
| **0041** | [The Epoch](0041-the-epoch.md) | Canon realization — the answer to "**which machine were you when this happened?**" (seeded by the 2026-07-25 outside review, loop 1; triaged + locked by JB same day; wire-honesty trio closed the same day, unblocking it): the **CanonEpoch** — a signed, content-addressed record naming the complete machine in force (Canon asset heads · plane version + profile · farm/stable ledger POINTERS · the adoptions whose signatures produced it · rollback parent · both clocks) · **the cut is a noticing, not a ceremony** — an epoch beat re-fingerprints the machine, diff-is-news; change WITHOUT an adoption behind it is the first drift signal · **enforcement points attest what they actually LOADED** (the worker its resolved assets, the plane its version/profile via /health) — silence becomes visible, never assumed fine · **drift is news**: lag wears amber and converges or gets loud; undeclared drift stages a finding at the human's gate (detection wears no levers) · **revert-to-sibling** — the first pre-authorized posture: rollback_parent's heads re-adopted as new siblings at a gate, nothing deleted, auto-revert REFUSED · the constitution-of-the-constitution closes: the epoch machinery is itself Canon, changed only through its own lanes · schema leaves the multiverse handshake room (0013). Built sp1–sp4 in one day (sp3/sp4 authored by Opus 4.8 under a safeguard swap, reviewed + corrected by Fable 5 same afternoon — findings D1–D4 in `agents/PROVENANCE.md`, the D1 self-accusation loop caught **standing live** and closed with `resolved_at`); proven by JB's key at every gate, closed whole `1c6b423`, era 0.41. | ✅ blessed · live |
| **0040** | [The Faculty and the Agent Lab](0040-the-faculty-and-the-agent-lab.md) | JB's "Orreth.adaptive.learning.universe.agent" (2026-07-23), documented in full: **the north star** — flow nodes carry NO prompts/skills; the Objective describes the need and the universe GETS the knowledge, CREATES the skill (gather → crystallize → canary → serve), and USES it · **the Faculty**: an installable, signed, trust-gated universe PACKAGE of adaptive ecosystems in four worn faculties (Deliberation: planner·critic·judge · Perception: analyzer·gap-detector · Conduct: router·optimizer·grader · Aegis: security — both faces, content-blind), INCLUDING the RAG ecosystem as a required component; packages are product · **the Agent Lab**: the human flow workspace built on 0008's reserved dual-mode authoring — tiles ↔ a LIVING ENGLISH SENTENCE, bidirectional; flows are Chronicle artifacts pinning faculty versions (revalidate on shift, never silent re-runs); execution is 0027's fingertip, no second engine ever; the librarian asks the unfilled objective and the plan gate holds. | 📘 **documented in full · BUILD PARKED** until the readiness stamp (JB's call, 2026-07-23) |
| **0039** | [The Chronicle and the Canon](0039-the-chronicle-and-the-canon.md) | Phase 2's constitution (JB's 2026-07-23 vision: "could be the most important element of Orreth"): **two books, one mind** — the Chronicle (Purpose Data: the O·I·O·T ladder, DAG-hard, walkable forever) and the Canon (Orreth Data: policies/prompts/skills/standards as versioned assets) — coupled in THE loop: Chronicle evidence → Canon proposals → human gates → the Canon governs the next Chronicle · **one substrate, two retention regimes**: Canon forever-versioned; Chronicle by record CLASS carrying allen's charter attributes — **the charter IS the storage map** (IAC allocates stores per class; records retention becomes provisioned physics) · locality + highest-score-survives + recall-frequency in the metabolism, its dials as Canon assets tuned on 0033 loss evidence · **the mentor/mentee graduation** (crystallize → canary at mentee tier → standings confirm — never silently dumber) · **memory-not-RPC law** (cross-field sharing via risen records; monitoring = a Chronicle reader) · the pointer law (bulk never enters the mind) · the projection privacy floor. ALL EIGHT locks landed 2026-07-23. | ✅ blessed · **WHOLE** 2026-07-23 — seed to living loop in ONE DAY: the registry & privacy floor (the smuggle test) · the Chronicle in the rows + the time dial ("the universe quoted its human back to himself") · the dialed metabolism w/ measured loss · the first graduation EARNED (and the refusal, and the demotion, all on the record) · the pointer law's handshake · 230/230 |
| **0038** | [The Stacks](0038-the-stacks.md) | Seven rows of the library (JB's design-2, 2026-07-22 — the seven RAG flavors as an ecosystem): **the one-truth law** — seven PROJECTIONS + flows over the one signed log, never seven stores (ingestion once; the purge reaches all; rebuildable = disposable) · commonality factored UP the cascade (eco assets on grace's shelf, minds at ada, tools at charlotte) — each field carries only its delta (naive = **baseline control** · rerank · multimodal · graph-on-Postgres · hybrid · router · swarm) · **the Dispatcher**: an unembodied organ in the put/get path, after the gateway, routing by a versioned standard the librarian tends — the organ enforces, the resident learns, every choice a record · **proving-ground-then-promote**: 0033 science + 0005 standings judge the rivals; winners join the planner, infra is earned by evidence · allen provisions — his first agent customer. ALL SEVEN decisions locked 2026-07-22 (seed dialog + AskUserQuestion, recommended paths). | ✅ blessed · **WHOLE** 2026-07-22 — seed to closed loop in ONE DAY: 4 spoonfuls · 7 embodied rows · the Dispatcher's ledger · the tournament-as-demo · **the first promotion: standard v2 argued by the standings, ADOPTED on JB's word** · 221/221 |
| **0037** | [The Estate](0037-the-estate.md) | allen, the cloud architect — the first resident whose **body is a tier** (the embodied field, off universe): control plane above, incarnation workforce below, master of cloud only. **Typed speech at his door** (0030 enforced: humans speak Objectives; agents speak Intentions/Observations with verified lineage — no Objective, no entry) · **the deployment charter** — gap analysis mechanical: RTO/RPO/classification/caching gaps refuse prod compile, route to answerers (human seat interim, JB-locked 2026-07-22), compound as memory, and form the birth-certificate backlog for future residents · **two DAGs always** — planned (rides the HITL card) and deployed (from CloudFormation's own graph); their diff is news · the Estate view: yaml as workshop assets, recallable in the glass · toolroom Shipyard-grown, CLIs as farm worldlines; custody never identity (spend = lifeforce, forecast on the card); plan free, apply gated, every apply carries its reverse · drift = the serials desk aimed at the actual cloud · first walk = brownfield adoption of the demo stack. Sets the base for design 2 (the seven RAGs — allen's first agent customer). | ✅ blessed · **WHOLE** 2026-07-22 — seed to standing resident in ONE DAY: 7 locks (all recommended paths) · 4 spoonfuls built & proven live · 3 JB walk findings reworked same-day · the brownfield walk fired by JB's own key (OrrethDemoStack 15 + JsbarthPipelineStack 19 resources, read-only, attested) · **the acceptance gate OPEN** · the embodied tier standing at `u:demo/f:allen` (:4510, the first universe-parented field) · windowed/exportable DAG + syntax-colored templates · 207/207 |
| **0036** | [The Living Brain](0036-the-living-brain.md) | The activity made visible (JB's fun-list ask, 2026-07-17 — "I want to see what Fable 5 can really do"): the Brain Glass taken from anatomy to PHYSIOLOGY, seven senses over the one truth (rule 7 by construction — every light is a record or request the glass already reads): **the synapse gap** — HITL made anatomical, staged requests orbiting a cleft only a human click can fire, a pressure ring that aches instead of alarming · **provenance beams** — knowledge entering the skull wearing the trust ladder (external `did:web` pierces from outside; quarantined flickers pale blue) · **consolidation** — the diaries visibly climbing the memory pyramid (JB's memory-levels lens, 0003) · the **gain dial** (off·calm·living·storm — loudness, never truth) · the ticker · metabolism and the replay scrub queued. sp1 LANDED + proven live same day; the hover-pause undone (the universe never stops turning). | 🟢 building — sp1 live |
| **0035** | [The Testament](0035-the-testament.md) | Survivorship — the universe that outlives its human (commissioned by JB's gravest lock, 0034 §8): **the testament** — the human's standing word about the end, a per-domain **fate map** (`seal · pass · shred`) on their own worldline, staged at 0012's gate while they live, revisable to the last day · **the passage** — a state machine where silence can only CONTAIN: the dead-man clock seals (reversible, loud), only attested death + cooling-off executes, and *the loudest abort is a heartbeat* ("silence never approves," kept at final stakes) · succession passes **custody, never identity** — the worldline closes, heirs narrow-never-widen the dead's consent, continuation is by graft-with-lineage · the only honest dead-man crypto-shred is **key mortality** (no escrow, no detector) · the survivors' door: the archive speaks *about, never as*; the legacy Brain Glass is a constellation, no fire. **All five §8 decisions locked by JB 2026-07-17**, every one on the recommended path; **all four spoonfuls built and proven the same day** — the whole arc walked live on a universe grown to die (sealed by silence · unsealed by one heartbeat · attested with quorum · the fates executed to the record · legacy kept, readable exactly as chosen). The era turns to v0.35. | ✅ blessed · **WHOLE** 2026-07-17 |
| **0034** | [The Continuity Universe](0034-the-continuity-universe.md) | The second brain's true form + the Vaulted Brain + **the Brain Glass** (JB's reveal session, part two): **Cognitive Continuity Infrastructure** — a governed personal universe preserving orientation, autonomy, and access to one's own history as biological recall fades; three 0009 templates over one substrate (Second Brain · Vaulted Brain · Continuity), clinical as a SEPARATE universe by design · the **label canon** — honest confidence in human words, composed structurally from record state ("this MAY have happened — two hints, not proof") · consent as dynamic state, delegated-authority role bundles, **survivorship staged for JB's gravest lock** · **the Mirror lands** (conversations assessed → profile observations + resident feedback) · the Brain Glass: ecosystems as brain-mapped regions, agentic activity as firing — a fourth projection under rule 7, legibility not neuroscience. | 🟡 draft — awaiting JB's read |
| **0033** | [The Physics of Memory](0033-the-physics-of-memory.md) | **The reveal dive** (JB, 2026-07-14): information theory made canon — *"we removed entropy from data/objects in time"* stated with scientific care: reconstruction uncertainty **bounded by contract, not accident** · Shannon's channel mapped to the organs (substrate = storage capacity; the aperture = channel capacity — never the same number) · the formulas bound to existing records: entropy · information gain · mutual information · **the Information Bottleneck as the aperture's objective function** · rate-distortion → **distortion contracts** on every distillation (must_preserve / prohibited_loss, refused at save) · **the coordinate** — the 0030 index's axes named (objective · intention · observation · thought · identity · scope · both clocks), landing soft (tags) before hard (contracts/v0 = rule-9 gate, staged WITH 0031's aperture) · the harness: infotheory.py metrics (reconstruction entropy, recall fidelity, context efficiency) — the science, runnable in demos. Third independent convergence: the outside review re-derived the aperture. | 🟡 draft — awaiting JB's read |
| **0032** | [The Serials Desk](0032-the-serials-desk.md) | Continuous acquisition — the knowledge loop's third mode (JB: "core to the universe's improvement system"): **a subscription is the human's standing word** (config-as-memory, staged at 0012's gate — a recurring spend is a consequence; out of fuel it pauses, never vanishes) · **the delivery beat** — the librarian's standing sweep: re-gather, dedup, admit quarantined as ever; the delivery note is the desk's worldline; quiet=log, news=medium · **the difference is the news** — same voice twice never promotes (0014 kept absolute); a changed claim at the same ref drops its old head to `investigating` (0031 §5's walk, trigger `superseded-at-source`) — contradiction v0 by identity, not meaning · **"…and keep it fresh"** on acquisition plans — one gate, both consequences. Zero new residents; the desk delivers, it never decides. | 🟡 draft — awaiting JB's read |
| **0031** | [The Aperture and the Workshop](0031-the-aperture-and-the-workshop.md) | Context descends with receipts, and the mind becomes visible (JB's context/data/memory session 2026-07-13; audits + adapts an outside architecture review — convergent with canon, adopted where new, rejected where weaker): **the aperture** — the context projection as a signed, content-addressed record (law + task + behavior pins + knowledge refs, hashes never bodies); every thought pins the aperture it ran under — restraint finally leaves receipts · **the shelf made whole** — prompts/personas leave the code and become versioned assets; **the Workshop**: the improver embodied (fifth room) with the shelf, version walks, approval packages, and the feedback door; human edits ride the same proposal chain · **the visible mind** — the `graph` panel kind + the work walk in the ledger (aperture → cycles → grade → outcome), rendered from records that already exist; scratch still evaporates · freshness triggers + reactivation rerank + the domain-package view. Zero new residents. | 🟢 blessed 2026-07-13 · building — spoonful 1 (the Workshop) live |
| **0030** | [The Human's Seat](0030-the-humans-seat.md) | The vision-sync dive (JB's interoperability session 2026-07-12): **Objective · Intention · Observation · Thought** — the four-rung ladder as canon (humans alone originate Objectives; each rung reviews the one below) · **the index IS the claim**: every act knows its whole lineage up the ladder, so the spacetime window becomes a WALK — any objective, any moment, total fidelity (the data scheme itself is JB's, held — rule-9 gate when it lands) · **the plan gate**: objectives are curated into HITL-approved plans before intentions fan · **the seat**: place-as-spine navigation, the Objective ledger (artifact-of-artifacts, recallable forever), inbox ≠ log, scoped tabs, legible beads · standing rule: every spoonful proven as a human in the glass. | 🟢 blessed · building |
| **0029** | [Multimodal Capability](0029-multimodal-capability.md) | The universe opens its hands (Universe-Brain §12, 3 JB locks 2026-07-11): **upload is an ask** — the librarian's card declares the drop zone, a file rides the parlor queue (bars: 256KB · txt/md/json/csv/pdf/png/jpg, past them the uniform refusal), and admission is two records: the **artifact** (content-addressed, `ingested-archive` — the store is modality-blind) and the **extraction** (knowledge derived from it, quarantined 0.0000) · **the parked eye**: formats needing vision are admitted honestly dark and their extraction intent PARKS (0014) — the retry list for the day a vision mind saddles at ada's gate (minds→Stable, converters→Farm, agents reach via skills) · plus the pinned-organ roster fix: a pin settles residency, honest zeros on quiet floors (rule 7). | ✅ blessed · live |
| **0028** | [Workspaces & the Improvement Engine](0028-workspaces-and-the-improvement-engine.md) | The glass grows rooms, the assets grow a gardener (Universe-Brain §8–§9, 3 JB locks 2026-07-11): **the card declares the workspace** (0020's decoupling kept) — ⛶ pulls the parlor full and the resident composes typed panels from state IT may read (stat · bars · list · doc, rendered blind); all four embodied residents have rooms; **medium+ markers wear amber** (0024's badge discharged) · **the improver** — one standing incarnation on the universe floor — reads the receipts (rollups, markers, parked intents) and proposes a **sibling version** of a behavioral asset, evidence cited; the governance seat computes the change kind **by diff** (never declared) and grades the lane: a **nudge adopts loud on medium**, a **rewrite waits for the human**; a no-op is refused, one open proposal per asset. | ✅ blessed · live |
| **0027** | [The Fingertip](0027-the-fingertip.md) | thought.graph made concrete (Universe-Brain §7, R7/R8 pre-locked + 3 JB locks 2026-07-11): **the universe construct IS the node graph** — an objective lands at a tier, its orchestration incarnation (factory-stamped, budget from the intent) plans HIGH and dispatches **slivers** down the request queue (one intent + budget slice + citation refs, never the whole); floors' fingertips carry them (chassis at the bottom, scribe-signed RunRecords); **review rides altitude** — the dispatching seat grades every return with a critic marker (0024 discharged) · **the human's request IS the top node** (no orchestrator self-confirms) · HITL inside the flow (a question parks its branch; silence is denial) · cross-eco dispatch-miss = **refuse AND ask** · templates are versioned artifacts in memory, refused-at-save; **standing jobs never complete** — the portfolio monitor beats on the universe floor. | ✅ blessed · live |
| **0026** | [The Purge](0026-the-purge.md) | Governed erasure (Universe-Brain §11, R9–R11 pre-locked): the engine was already in the core — this dive builds the doors. **The split model (R9)**: personal-data erasure rides the subject's own consent (`forget about me:` now walks each silenced claim through the plane's token-guarded `/tombstone` — stub stripped, bytes gone, `purged` table boot-restores so a restart never resurrects readability) · **the operational path stages and HOLDS**: bars are absolute (0012 §5) — "held: quorum 1 of 2 — containment active; destruction waits for humans, plural" · **the seal (R11)**: staging darkens the named refs at machine speed, reversible, never conflated with the purged set · **the walk crosses the promotion boundary (R10)** · immunity: gather refuses a recalled source at the door — the door remembers the infection. | ✅ blessed · live |

## How we work a dive

1. Draft the spec here (schema + rationale + **open decisions** flagged explicitly).
2. JB reacts / pushes back on the open decisions (vision-owner's call).
3. Lock the decisions → record them in `../decisions/`.
4. Only then does code get written against the blessed spec.
5. When the dive closes WHOLE (built, proven as a human in the glass), the closing
   commit bumps `/VERSION` to the dive's era — `0.39` for 0039. The console wears the
   era (`dev.sh` derives `ORRETH_VERSION` from it), so a close without its bump leaves
   the glass claiming an older world. Standing rule since 2026-07-24, after the third
   out-of-band correction.
