# The Honest Boundary — the standing register of claims, proofs, and parks

<!-- PROVENANCE: Fable 5 (claude-fable-5) — seeded 2026-07-26 from the outside
     poster's §12 (ORRETH_POSTER.md, Sol 5.6's evidence-based reading), at JB's
     word: "these sound like solid ways to help prove claims of orreth when we
     start creating universes." -->

Orreth's credibility is not that everything is built — it is that the line
between proven and promised is **written down, witnessed, and kept current**.
This register is that line, in one place. Every claim an outsider might test
gets a row: what stands proven and *where the evidence lives*, what is
reference-proven or deliberately partial, and what is designed but parked
behind a named gate. When a universe is created for someone else, this page is
what we hand them before the demo.

Maintenance law: **a dive that moves a row updates this page in its closing
commit** — a register that lags is worse than no register, because it lies
with confidence.

## 1. Proven, with the evidence named

| Claim | Evidence |
|---|---|
| **A capability is a sealed, signed box a stranger installs through their own human gate — never a folder copied into a checkout (KCR-0002 PAID, the last KCR)** — `orreth-package-v1`: identity = the hash of the canonical declaration bytes; provenance = the publisher's self-certifying Ed25519 signature, verifiable offline; crew = container images only (the checkout era's shell words refuse at pack AND verify); the receiving vocabulary enforced (a beyond-canon room refuses naming the release law); the gate card carries everything the human weighs; the discoverer re-verifies installed boxes at every boot; un-install is the standing governed retire | 0072 sp4 (2026-09-12): the stranger walk WHOLE — the box packed by a persistent publisher identity outside the repo; the tampered twin refused at the door naming the byte law; staged → approved → installed; then the checkout folder REMOVED and the worker rebooted: «installed from a published box», all five rooms served, the glass whole, an ask answered. Six format laws; suite 595 | no shipped box carries crew yet (the lane is law-tested + deed-wired, exercised vacuously — E-RAG asks for no processes); a box declaring a NEW floor scope awaits a hull-provisioning walk; publisher trust is signature-only (the human attests the publisher, no registry) |
| **The machine is watched per style from the one truth, and the API is the same door shown raw** — the `monitor` room folds the SAME records the scoreboard reads (volume · nearest-rank p50/p95 · context spend · draft counts · every ref a door · this world's raised hands); the `api-console` shows the exact wire shape any client sends, the curl to take away, and the envelope exactly as it returns (door-to-done and walk latency split honestly; the draft lane visible raw); a changed install source now UPGRADES the manifest's shelf head as a lineage sibling preserving human state | 0072 sp3 (2026-09-11): live — the operate room measured the boot-lap starvation where the human can see it (naive p50 1.3s / p95 17.5s); a draft ask fired through the raw door returned its envelope wearing {hash, knobs, note}; the upgrade lane landed the five-room manifest after the plant-once wound blocked sp3's own rooms. Suite 589 | NAMED PARTIAL: a budget-capped retrieve that misses the shelf head degrades to blind replant (KCR-0002's signed box replaces the road, sp4); guardrail audit counts stay with vigil + the Observatory; per-style assay doors wait for honest volume |
| **The workspace is a better face on the same doors, never a bypass — and a draft experiment is always traceable** — the `workspace` panel kind rendered blind from a declaration (the engine kernel-reusable, JB's lock 3); the roster = registry (firmware) + shelf heads (craft) + standings (records) through one door; every edit travels the one-motion craft-edit gate; drafts are held to EXACTLY the landed law (the registry's gate teaches), ride the experiment's in-memory head-swap on a disposable node, never touch the ask-cache, and the exchange record wears the draft {hash, knobs} + tag while the envelope confesses | 0072 sp2 (2026-09-11): live in the glass — Run through the real ask door (Result whole: variant · law · latency · exchange · citations); Compare on advanced (a wide_factor-3 draft cast a visibly wider net, both columns wearing receipts; pg receipt: the draft hash inside the exchange body under the `draft-config` tag); «version it» landed the gated sibling with the comparison note and the roster re-served tuned:true; a bad draft refused naming the declared knobs and the blast. Suite 587 | State/Timeline/replay-bench/pins/standings-strip/presence = workspace.md's named later steps; streaming = JB's lock 1's later door; entitlement-scoped edit controls = sp5; the canvas lights whole-run (per-stage records inside a flow don't exist — guessing would break the live-watch law) |
| **The rich rooms are kernel glass: a capability DECLARES its rooms and the kernel renders them blind — no foreign code, ever** — `chat` + `sources` joined the panel vocabulary (a vocabulary release under 0055's law); E-RAG's genesis is pure data (a suite law loads it as literals; the selector held to `variants.MENU` by a drift-guard); the chat canvas is a worldline at the ask door (server-minted head, every turn deriving from [choice, thread, prior], threaded asks never cached, every answer wearing variant + guardrail law + exchange ref + citations as doors); the sources room is the Farm's own truth + the Basket's signed-log progress | 0072 sp1 (2026-09-11): live in the glass — both rooms rendered from the declaration alone; a two-turn walk (T1 «naive» + thread minted → T2 riding the same head, pg receipt: all three refs on the turn); four glass wounds caught by the walk and paid in-hour (rooms render without reports · the watcher's give-up became the ⟲ keep-watching door, proven twice · one wait-line law · the composer survives every rebuild). Suite 581 | the folder-install road remains the KCR-0002 wound (sp4); workspace/monitor/api-console kinds are sp2/sp3; serve latency wobbles 30s–5min under boot metabolism laps — the middleware season's wound, named |
| **A human is an identity: their own key, their own signed acts, their name never reissued — and anonymous is a respected state, not a hole** — persons minted at becky's gate (seed persisted like every self, NANDA-registered, becky-signed registry row); strokes, preferences, withdrawals, and thumbs sign under the person's own key; one visibility law (a person sees their strokes, the anonymous portrait never leaks them); custody and registry must agree or the seat refuses | 0070 sp5 (2026-09-11): live — «jb» minted through a real gate card; his stroke landed with AUTHOR = his own DID (the node verifying a human's signature for the first time); portraits cleanly separated; his 👍 wearing his DID; custody surviving a worker replant mid-walk | attribution trusts the operator's console v1 — session-bound auth is 0072's workspace |
| **The scrub has two honest clocks: events by the dial, beliefs by versions-as-time** — when-words resolve deterministically or refuse BY NAME (never a guess); a when-word in the window-ask frames the events window to that day; «what did we believe <when>» replays the claim worldlines to the cutoff from records alone, answering believed-THEN and retracted-SINCE with full evidence refs | 0070 sp4 (2026-09-11): live — «three days ago about rammed earth» answered exactly (and out-honested the walker's own timezone); «recently» refused by name; 8 claims listed with signed evidence; «🕰 the window heard 'yesterday' — framed to 2026-09-10»; the retracted-since flag proven at controlled clocks. Suite 574 | day-granular cutoff (sub-day stories hide inside it, named) |
| **How a resident sounds is the human's versioned craft, and the honesty floor is tighten-only** — persona-* assets (Universe base + expertise-reflective residents) worn via the ⟦persona⟧ slot at the one choke point; the base's honesty line rides every composed voice verbatim; persona = PURPOSE by the L2 amendment (prod-editable for ALL residents, ability prompts staying firmware); the Rust blurbs serve from the speech shelf with the literals as genesis | 0070 sp3 (2026-09-11): live — one question, two unmistakable voices (the stable-keeper beside the astronomer); persona-ada edited through the one door into a harbormaster and her next answer sailed, the honesty floor riding the new idiom; the old version behind her; a shapeless edit refused by field name. Suite 570 | unvoiced organs' personas join when their voices do |
| **The profile is read at every voiced answer — sovereignly — and the privacy floor holds in EVERY projection** — the slice composed through the sovereign door, provenance-labeled (you told me / I observed / the mirror noticed), injected structurally; typed preferences only a human's own word may set, latest wins, language honored by the voice; the floor's tag list derives from the Canon registry and rides the embeddings worklist, the door evicting what the old blindness left | 0070 sp2 (2026-09-11): live — one voiced breath IN SPANISH carrying the asserted facts, the typed preference, and the observed strokes; the withdrawal erased the claim and the next reply returned in English mid-thread; 165 sovereign vectors evicted across the floors, zero standing, excluded from every future worklist. Suite 566 | the dispatcher's question lane stays unvoiced by the standing law — its profile-aware turn rides the attributes lane when demanded |
| **A conversation is a worldline, and a reply remembers** — the thread's head is a first-class signed record minted server-side; every turn cites the head and the turn before it (lineage on the record, the prior ref in the body); the voiced lane composes recent turns from records — never the browser — under the thread-turns dial; a thread rebuilds from records alone through its own door | 0070 sp1 (2026-09-10): live — «my favorite wall material is hempcrete» → next voiced reply: «Hempcrete — you just told me that in this thread», with the scope honestly named; the rebuild door returned the talk in chain order; pg: turn 1 ← [head], turn 2 ← [head, turn 1]. Suite 562 | the other audience lanes join at sp4; cross-life restore is 0072's affordance |
| **A database is a governed read-only citizen, and the embedding standard tells one truth** — sqlite through the same two-keeper card and metered door (mode=ro AND a one-statement SELECT gate, one face); the stacks-embedding standard declares model+dims (multilingual by L3); every vector wears its model, the worklist lists wrong-model rows, the chunk policy wears the model — a swap re-embeds the world paced and measurably; the resting state twinned into the Python reference | 0069 sp5 (2026-09-10): live — site-materials.db onboarded→serving; schema + SELECT served, INSERT/PRAGMA refused one-faced; THE GREAT RE-EMBEDDING measurable in pg (31,525 «pre-standard» by name, 291 re-embedded in minutes, 64/beat). Suites 558 · 24 | named: fastembed's pooling for this model changed across versions — a library flip shifts vectors without a name change (in the standard's own note); PostgreSQL/MySQL declared growth |
| **The machine keeps an encyclopedia where every statement is a claim chained to signed evidence, and an origin's death runs through it** — origin/concept/catalog pages as derived rebuildable records; links whitelisted against the real; the cascade reads 0026's seals (never probes a door — blindness can't cascade a living origin); a flagged page refuses «current» until a recorded rebuild that names the retraction; whole documents feed the Hierarchical tree through a railed hook (refused text never rows, masked spans never serve) | 0069 sp4 (2026-09-10): live — claims/pages/catalog building on the beat; a real purge seal fired the cascade (1 claim retracted → 4 pages flagged → rebuilds naming it); the walk's own wound (raw text entering the projection around the ingest rail) caught and paid in-hour, tainted rows truncated. Suite 553 | the capped pull still gates which pointers chunk — import-time cut seeded |
| **Every mainstream document format yields deterministically, the rails run before knowledge exists, and the parked list is finally the retry list** — PDF/Word/Excel/PowerPoint/HTML extract firmware-versioned with lineage (a paid park's knowledge cites artifact AND park); 0068's rails run at ingest (a refusing rule mints NO knowledge; the refusal record carries events, never content); failed parses and eyes-needing formats park with their flaw or missing eye NAMED | 0069 sp3 (2026-09-10): live — a Word import extracted (docx-extract-v1) beside a card-number doc REFUSED at the ingest rail in the same job (audit on the log, zero digits anywhere); the morning's dark parks swept: the fake PDF honestly unpayable, the 1.1 MB cloud PDF PAID (100,391 chars, chain whole to {store, key}). Suite 545 | parked honestly: OCR/ASR await the Stable's eyes |
| **A source of records is a governed citizen: chartered by allen, wired by charlotte, alive only while it earns its beats** — the Farm's `store` kind (S3 + on-premise) on the one unchanged lifecycle: manifest = declared operations, ambient credentials (a key in zero records), traversal one-faced, resting refuses at the meter for free, a changed operation set quarantines; one human word mints the charter AND plants the wire; a moved store lands a signed freshness observation | 0069 sp2 (2026-09-10): live — two stores onboarded on one word each (on-premise + s3://jbiotlodgebucket/documents); serving earned on beats; the first cloud import (1.1 MB PDF) landed content-addressed with its pointer citing {store, key}; freshness proven on a dropped file; rest→free refusal→resume walked. Suite 537. Azure/GCS: recognized, named, honestly unwalked |
| **Bulk never enters the mind, and a crashed import resumes from the log alone** — the Basket imports folders/many-files/zip-members as content-addressed objects + signed pointers carrying each entry's origin (0039 §6 made the ingest path); the import queue IS the signed log (job record before any byte; progress derived; completion names interruption); the picker walks only a declared root, traversal one-faced | 0069 sp1 (2026-09-10): live — /etc refused; 4-entry import whole (objects + pointers + no mass in any body + honest dark PDF); the crash walk — worker killed mid-carry of 8, replanted with zero memory, the log finished 8/8 with zero duplicates. Suite 529 (ten Basket laws). Upload's old one-file-256KB bar: now the human's dials |
| **Scary never lands on an ordinary word, never governs in silence, and stops in one click** — an empty/weakened guardrail set HOLDS at the gate until a human stamps it (consent grammar: minted from the click, windowed by dial, the record cites the card); every answer served under it confesses in the reply, the envelope, and the glass banner; the walk-back is ungated and lands a revoked sibling + a restore through the one door; a revoked or lapsed stamp stops governing at the read | 0068 sp5 (2026-09-10): the full lifecycle live — empty set held → stamp card wearing the flaw → approved → landed under the stamp → the morning's masked SSN ask passed UNMASKED with the confession in the reply → one click → grant→revoked on the worldline (pg receipt) → the same ask masks again. Suites 519 · SDK 36. Same motion: the bell never again asks beside a standing word (three patient looks · durable declines · ghost asks withdraw themselves — proven on req-1909) |
| **The rails bite, and the audit never leaks what they caught** — PII/PCI detectors are firmware (Luhn-checked cards; SSN/email/phone) enforcing the craft rules in the one lane every thought passes: inputs before the model call (a refused exchange spends nothing), outputs before they return (spent tokens stay metered); every checked thought lands a scribe-signed guardrail-audit either way, clean or hit, carrying categories, counts, actions, and reasons — never the content; quarantine stages vigil's card citing the audit | 0068 sp4 (2026-09-10): live on the rig — a card number refused at the ear with the rule's reason as the reply; a real SSN masked inside ada's actual answer; mask→quarantine turned through the craft door (law re-addressed in the breath) → masked + vigil's card; walk-back re-addressed to genesis; the log holds all four outcomes and the quarantined audit's body, read through the governed door, holds the SSN nowhere. Suites 517 · SDK 36 (rails parity twin) · Rust green |
| **No thought serves without naming its law** — the gateway demands the current ResolvedContext id on every authorize (missing and stale wear the one face; the off-switch is explicit and confessed at boot); the grant returns the pin and every meter line carries it verbatim, so «which guardrails governed this answer» is one SQL join from the fuel ledger to the rails; the SDK refetches once on a stale refusal — the fresh law, the same ask | 0068 sp3 (2026-09-10): the smoke — no-law 403 · stale-law 403 the same face · the true pin accepted; live — a real governed thought served under the demand, and the join read back «sonnet-5 under gr-4a7de4a1…» with a not-yet-pinned floor honestly at «pre-worker». orrethd 13 + suites 503 + SDK 24. Named honestly: published 0.3.0 cannot pin — 0.4.0 rides the dive's close |
| **The law a thought runs under has a name, a signature, and a spine** — every plane composes its content-addressed ResolvedContext at boot (floors + the guardrail pin), persists every version to Postgres, and re-addresses within a breath when the rails turn; standards ride becky-SIGNED bundles the plane verifies before serving and a child verifies against its own pinned root — a poisoned standard kills the boot, an unsigned pull confesses; a local floor that weakens an inherited one refuses the whole boot with the teaching; and `floors` is finally DECLARED in the sacred tier-profile contract (JB's rule-9 word) | 0068 sp2 (2026-09-09): live — «ResolvedContext composed» at every plane's boot; becky's bundle signed, live-verified at the push door, served to pullers; the context re-addressed with the rails' pin; three floors' contexts durable in Postgres. Suites 503 + 24 (parity) + orrethd 12 (four new boot laws). Named honestly: the child's own VERIFIED boot line lands at the next rig cycle; the bundle is re-pushed per worker life; sp3 makes the gateway demand the id |
| **The rails have a grammar, a lattice, and a living version** — a guardrail is a written rule (match · action on the strength ladder · a mandatory WHY) in its own gated drawer; PII-masked and PCI-refused ship as genesis; a capability set may add, widen, or strengthen and a weakening REFUSES at the door with the teaching; absence never removes; an empty set is legal-explicit-scary awaiting the stamp law; and the ask-cache keys on the LIVE composed set's hash — a turned rail revalidates every cached answer by construction | 0068 sp1 (2026-09-09): live on the rig — genesis planted on the first ask; a weakening refused with «a child tightens, never loosens»; a lawful strengthening landed; the caching charter law walked whole (cached → the universal tightened through the one door → the same words FRESH). The walk's own find fixed in-hour: the pull now carries guardrail-* so the planter never re-plants beside a human's word. Suite 503 (eight grammar laws) + Rust green. Named honestly: detectors and enforcement arrive sp4; Rust's resolve-side check sp2; the stamp sp5 |
| **The answer wears its thinking** — a live ask's whole picture projected from its signed records and nothing else: the asker, the router's choice (dooring its record, its why read aloud), the style worn, the answer with its signals, and every judgment landed since; the picture GROWS because records land and degrades honestly when they leave (a purged choice's node is absent and the story says so); served on a click and drawn beneath the answer in the reader | 0067 sp4 (2026-09-09): live on the rig — an SDK ask, then /act: format act-v1, the chain you→choice→style→answer whole, validate() flawless, the story reading the router's why; the glass serves the hook. Suite 495 (three accretion laws incl. verdict-grows and purged-degrades) + Rust green. THE DIVE CLOSED WHOLE — KCR-0004 PAID |
| **Four graph dialects, one language** — the atlas's door grammar absorbed as the format's own (record·room·floor·view·resident·parlor·world·brain); the atlas converts with legacy keys riding inside the act door (the standing sheet loses nothing); the estate converts with allen's DAG polish PAID — every card opens the template asset record that declared it (what you can click is what you signed), unknown refs dooring the view honestly; one export idiom across the lanes | 0067 sp3 (2026-09-09): live — the /atlas door serves format act-v1, becky's door {kind: resident}, the first edge's glow list [join, field-join], the sheet unchanged. The estate side suite-proven (deed doors · honest fallback · categories survive); its live receipt lands with allen's next deployment — named, never faked. Suite 492 + Rust green |
| **Every box in the capability flow is a door** — the one node family that never was one (`dkCvNode`, referenced since 0055, never defined) now opens: a run stage opens its OWN record through the standing reader; an unrun stage says so in plain words on an honest card — never a fake ref, never a dead click; stage rows carry their record refs at the compose; the manifest-flow dialect converts to act-v1 (run stages door records, unrun ones door the room, groups survive); the one drawer's dispatcher routes pipelines to the serpentine lane with all its earned goods intact | 0067 sp2 (2026-09-09): live on the rig — 48/48 desk stage rows carry refs; a stage's door walked end to end (retrieve-context → the cross-floor door → the real record on f:charles, digest «prior report: yes · 9 lesson(s)»); the glass serves the doored pen. Suite 490 + Rust green. Named honestly: the room-door is a card until the tab router earns a door grammar; the glass twin mirrors the sim converter by discipline |
| **One graph language begins, and the deepest answer is finally read** — the act-graph format (act-v1, v1-earmarked; the sacred v0 untouched): every node a DOOR or the picture is refused at validation, the narrative a bijection a human reads aloud, edges keeping the glow law; the choreography walk converted first with nothing lost, all three worker sites emitting it; the glass's drawer renders it with any doored box opening the real record; and the APERTURE — what a seat could see, whole — opens by coordinate through a governed door after sitting unread since 0031 | 0067 sp1 (2026-09-09): live on the rig — a filed objective's staged plan returned format act-v1 with every node a door and validate() flawless on the wire; the aperture door served a real July envelope by coordinate (law hash · task+budget · behavior profile · seat); the glass serves the new pen. Suite 489 (five format laws) + Rust green. Named honestly: the aperture hunt is serial-on-a-click; pre-0033 envelopes wear no coordinate and the miss says so; dialects two through five migrate one per spoonful (L1) |
| **Exploration lives inside the human's word, and the router's old row rests on the record** — a dialed share of Auto asks deterministically tries an unchosen style, ALWAYS confessed on the choice record (instead_of + the plain why + the style's tag), never over the asker's word or a spent thought, and the 20% ceiling is FIRMWARE — the dial can never widen the bound set in advance; three router dials (replay cadence · the proposal floor · the slice) on the 0063 machinery; the PUT side derives by registry and modality (never "everything"); the router row retired honorably as one librarian-signed record naming its absorber and where its tactic lives on | 0066 sp5 (2026-09-09): live on the rig — the slice dial turned 5→20 at the UNIVERSE'S own door (the floor's gate refused first — 0063's ladder law holding unprompted), the slice explored within a dozen asks («multimodal» instead of «naive», confessed), canon restored; the retirement record read back from Postgres. Suite 484 (six slice laws incl. the 99-behaves-as-20 bound) + Rust green |
| **The scoreboard reports, argues, and never referees** — per question-kind × style, a Beta posterior over reward with credible intervals (approximation named), rebuilt whole from the records and purge-reached by construction; a human's 👎 is refused from the arithmetic, counted aside; the replay harness panels every unchosen arm at zero serving risk (one signed record per ask, bench-only — latency and thumbs honestly unreachable); the proposal generator stages a standard revision ONLY when a challenger's credible floor clears the incumbent's ceiling with volume — onto the existing improvement road at the human's gate, refusing thin evidence and unmeasured incumbents alike | 0066 sp4 (2026-09-09): live on the rig — the first breath replayed an ask through the arms, stood eleven cells, and proposed NOTHING (one sample argues nothing — the refusal holding on the wire is the proof); the suite proves the staging side (a clear winner stages with its evidence while the standing rulebook stays untouched). Suite 478 (seven standings laws) + Rust green. Named honestly: the first real proposal reaches the gate when accumulated play clears the deliberately high bar — the beat stages it unprompted; cadences and MIN_N dial at sp5 |
| **Signals are data, the reward function is law — and a thumb is never a reward scalar** — every completed ask writes its cheap bench into the exchange record (retrieval · faithfulness · answer · coverage, declared proxies) beside measured latency and context cost; the accreted vector assembles by WALKING the records (vera's verdicts and the human's thumb join by the refs they carry); the combining weights live in the routing standard (re-weighting is a gated act; history re-scores under any future weights); vera's tier replaces the answer proxy when she speaks; 👍 and 👎 produce the IDENTICAL score — a 👎 vetoes the sample, never tilts it; the Observation rung exists (chassis observations scribe-signed, vera sampling all three rungs) | 0066 sp3 (2026-09-09): live on the rig — an SDK ask's envelope carried latency 991 ms · context 458 chars and the exchange body read back its whole bench. Suite 471 (eight signal laws; the L3 test holds sycophancy refused BY CODE; a verdict lands at every rung) + Rust green. Named honestly: rung proxies are declared approximations refined by vera's tier on her own cadence; the third rung's first live record lands with the next chassis cycle (suite-proven today) |
| **The router reads before it routes, and thinks only when it must** — a declared, versioned featurizer (feat-v1, pinned in every choice) emits the analysis dimensions in plain words: kind · parts · length · domain · freshness · modality · latency preference · confidence; only an ambiguous read from an asker who did not demand speed earns ONE governed thought (typed contract over the eleven-name menu, one re-ask, an honest park with the deterministic read serving); the asker's word outranks the mind; `latency: fast` is the first HONORED attribute, confessed in the envelope | 0066 sp2 (2026-09-09): live on the rig, three lanes — an ambiguous ask consulted a real governed mind («multi-agent», a plain-sentence why, all on the choice record); the same ambiguity with latency:fast spent no thought (honored=['latency']); a clear ask stayed hot. The park law fired unprompted on the first live consult (a pin/class mismatch) and served exactly as designed. Suite 463 (nine Analyzer laws) + Rust green. Named honestly: the consult rides the judge mind at medium (a cheap router mind is ada's allocation call); confidence thresholds tune when sp4's standings show misjudgment |
| **Every routing reward can reach its action** — the RL substrate's missing join is real: a dispatch choice derives from the exact rulebook version that made it, wears its coordinate as tags, and names its featurizer version; the answer's exchange record derives from the choice; a thumb or verdict on an answer walks to the routing decision in ONE lineage hop on the plane's standing index (genesis-only worlds leave lineage empty, honestly) | 0066 sp1 (2026-09-09): live on the rig — an SDK ask, then one SQL join walked exchange → choice (coordinate tags read back; the body through the governed door confessed flavor · featurizer shapes-v0 · the why · standard_ref; the choice's own lineage resolved to the routing-standard asset). The yardstick's rows now keep the whole choice ref; the parlor's routing note stopped truncating it. Suite 454 (four join laws) + Rust green. Named honestly: the parlor audience record's structured lineage is a small follow-on (the whole ref rides in its text today); learning stays Stage 0 — everything logs, nothing learns until 0066's later spoonfuls walk their gates |
| **The selection is never a secret, anywhere** — which retrieval style served an answer is a field on the envelope, a TAG on the signed exchange record (`variant:<style>`, the arm-tag pattern), a spoken line in the glass's own reply, and a column on the fuel ledger (`variant` on the meter line, kept verbatim by the plane — per-style cost is a query); a mind's caller attributes any thought to a style per call (`_variant`, riding the `_klass` seam); the warmth tap expires by the recall-days dial — declared retention where an unreachable file used to live | 0065 sp5 (2026-09-09): live receipts read back FROM POSTGRES — the meter line `hyde · sp5-walk · 5` and the record tags `["ask", "variant:graph"]`; the parlor's reply to a human spoke «the dispatcher chose "naive" …» with the whole choice ref. Suites 450 + SDK 24 (parity) + Rust green. Named honestly: the Observatory's per-variant cost view waits for demand (the ledger holds the data now); diary/run records untouched (validated schema — its own motion) |
| **THE MENU ANSWERS WHOLE — eleven styles declared, eleven serving** — the five new compositions scaffold-first from standing machinery (a long document answers from its SECTION; a retrieval plan lands as a signed record and every hit names its step; remembering confesses what it added; search rides the imagined answer; a weak catch takes a visible second look); the tournament's field DERIVES from the registry and grades latency + cost beside the science axes (0038 §4 paid); every row reads its style's craft at the door — a Workshop knob takes effect where the work happens | 0065 sp4 (2026-09-09): live on the rig — all five new styles served AS THEMSELVES through the SDK (no baseline falls); the knob walk: variant-naive k turned 4→2 through the one-motion door and the very next ask served exactly 2 citations, canon restored. Two walk-finds paid in the hour: a plan echoing into its own projection (machinery-talk is not memory — the lane law grew the exclusion) and the pull silently dropping variant craft. Suite 450 (five per-variant law files: four canonical laws + one delta each) + Rust green. Named honestly: the scaffolds confess their stand-in minds (drafting · planning · worldlines — each saddles in its dive); graph hops declared-unthreaded until demand |
| **The graph lives in Postgres, and every edge carries its witness** — terms as nodes, within-span co-occurrence as edges, each edge naming its record · span · the piece's hash (the citation IS the edge's provenance); rows wear the extraction law's version (extractor × chunk policy — either turning re-cuts visibly); the walk reads ONLY inside the ids the caller's retrieve authorized (never a second read path); purge forgets what only the dead record knew, in the tombstone's own breath; ONE term law shared by identity between rebuild and standing sweep | 0065 sp3 (2026-09-09): live on the rig — f:prod extracted 253 records in one sweep; an SDK ask with `variant: graph` answered from Postgres («the graph walked 4 witness(es) in Postgres — no rebuild», 3.0 s, the binding pair on the reply). Suite 424 (eight graph laws incl. the cross-document edge and the authorized-set law) + Rust green. Named honestly: the extractor is the deterministic scaffold — real entities/typed relations arrive with a saddled extraction mind bumping the law version (0069's minded eyes); hop depth 1 until sp4 threads the variant's knob |
| **An ask reads a STANDING shelf, not a rebuilt one** — the chunk/tree projection lives on the plane (pointers into derived text, never blobs; chunk-grain vectors; tree parents for long documents; every row wearing the policy hash that cut it), filled by a per-floor sweep with ONE knife shared with the rebuild (the lane law extracted — identical by construction), read only over ids the caller's retrieve authorized, hashes re-checked at serve time, and evicted in the same tombstone breath as a purge | 0065 sp2 (2026-09-09): live on the rig — f:prod's sweep cut 253 material records in one beat, the next touched 1; an SDK ask answered from the shelf («standing shelf served 4 hit(s) — no rebuild», 3.1 s, citations riding); the first live sweep CAUGHT ITS OWN WOUND (the global worklist marked records the capped pull never saw — the sweep now judges only what it can see, the poisoned rows truncated because a projection is rebuildable). Suite 416 (nine chunk laws) + Rust green. Named honestly: baseline row only until sp4 threads the rest; temporal asks take the rebuild; the confession floor is embedder-relative (sp4's harness measures); tree rows stand unwalked until Hierarchical |
| **The eleven retrieval styles are DECLARATIONS, and the menu never lies** — each style firmware-declared (composition over the six module boundaries, requires, cost class, its delta in one plain sentence, its genesis knobs); the servable list DERIVES from registry × standing flows (hand-kept copies retired); a declared-unbuilt style falls to the baseline loudly on the record; a style's numbers are craft in their own drawer, gate-checked before landing with teachings on every sibling | 0065 sp1 (2026-09-09): live on the rig — «advanced» served wearing its canonical name over the rerank flow; «hyde» (declared, unbuilt) fell loudly; «quantum» refused naming the menu; the craft gate refused an undeclared knob with the teaching and landed a clean turn canonical. Suite 407 (nine registry laws). Named honestly: a landed config head takes effect when sp4 threads config() through the flows; five styles await their sp4 scaffolds |
| **An answer serves again only inside the human's word, always confessed, never a second truth** — the ask-cache is a projection: same canonical words · same variant · same floor · same guardrail-set version (all four in the key, so a changed guardrail set revalidates by construction), every entry leaning on its signed exchange record by whole ref and checked ALIVE read-side before any hit serves (purge reach with no hooks); every hit confessed in the envelope with the original exchange as the judgeable ref; the TTL a ladder-homed dial (`ask-cache-ttl-s` 300; 0 = the closed cache); grace-served answers never cache | 0071 sp5 (2026-09-08): the live walk on the rig — a cold ask fresh; the same words in different clothes (case, whitespace) served from the cache WITH the confession and the original exchange ref; the dial turned to 0 through the one-motion craft door and the same words answered fresh with a new exchange; canon restored. Suite 398 (eight cache laws). Named honestly: matching is same-words (meaning-similar serving awaits its own lock); the live guardrail turn arrives with 0068 (the law is suite-proven — the version lives in the key); a live purge is a human's gated click |
| **Knocking is metered, per identity, inside the operator's ceilings** — every boundary door counts knocks per DID in a fixed window (plane and worker both); at the ceiling the answer is one honest face («the door is busy for you») with a truthful `retry_after_s`; one caller's flood never slows another; body size is a deliberate, dialed ceiling (413), never a framework accident; the ceilings are governed dials (`rate-per-min` 240 · `body-limit-kb` 2048) — declaration firmware, value the human's craft | 0071 sp4 (2026-09-08): the traffic smoke on a strict plane (ceiling 5, body 2000) — eight knocks served exactly [200×5, 429×3], a second identity rode through the flood at 200, the busy face named its wait, a 4 KB body refused 413; the default-ceiling smoke re-ran whole (240/min never bites an honest walk). `orreth_sim/traffic.py` + `knock()` in `main.rs` (five doors); suite 390 (six traffic laws) + Rust green. Named honestly: fixed window (burst-at-the-seam accepted), the worker's book per-process |
| **A stranger's world completes a governed join with the door and the SDK alone** — the root signs the door's credential once, offline, and never enters a serving process; the door holds only its own key and mints root-chained leases the plane verifies as it always has; the standing welcome is a signed record on the floor (no private file), and becky's side writes the admission record it granted | 0071 sp3 (2026-09-08): the stranger walk — a fresh kernel + the SDK's `joindoor` (mint · serve · pending · approve), no worker, no repo imports: one governed join end to end (nonce challenged, key proven, staged, the operator's word from their own terminal, a lease that remembered and recalled), then a second life honored by the welcome RECORD with no click owed and the WHY kept on the final resolution. Suite 384 (five delegation laws); PyPI/compose/book growth rides the dive's close per the release-alignment law |
| **Machines ask and are answered, structurally** — one `ask` kind for agents (and the API to come): signed with the asker's own key (never a bearer on the public queue), verified against the DID and the standing welcome, answered with a structured envelope whose refs are WHOLE (reply · citations · the variant that served · the full choice record id · the judgeable exchange) — the selection never a secret, an unbuilt or unknown choice refused with a teaching, a failed signature a forgery in any mode | 0071 sp2 (2026-09-08): the live walk on the rig — scout-from-outside rejoined on its standing welcome, read the envelope (Auto chose «rerank», the choice ref opens), chose «graph» and the reply confessed it, and the forged ask (an imposter signing the victim's DID) met the one face — a hole the walk itself caught on its first run (the dev-grace lane had served it) and the same hour closed. `orreth_sim/askdoor.py` + suite 379; SDK `ask()` |
| **The queue's word is authorized, and the fuel ledger answers only to its own key** — a resolution demands a root-chained token carrying the narrow `resolve` action (the pen; JB's rule-9 word added it to the token grammar so answering the queue never requires the shred-lever class); the one tokenless step left is the joiner answering becky's nonce; terminal statuses are immutable and transitions are law; `/model/meter` demands the same lease `authorize` checks, and only the token's own subject may be reconciled — the credential proves the door and never enters the ledger | 0071 sp1 (2026-09-08): the live smoke suite on a fresh plane — forged approval, forged lease result, and a retrieve-only lease each refused with the one face and the card unmoved; the pen resolved; the tokenless proved lane served; `denied → approved` refused with its teaching; tokenless metering and metering another's line refused; own-line-own-key served. 371 Python + all Rust crates green. The glass rides the worker's `/resolve` door; capability specialists and trusted crew re-mint with the pen at becky's welcome; strangers' leases stay retrieve-only |
| One recursive binary runs any tier; profiles supply behavior | the dev rig: universe :4500 · eco :4501 · field :4502 + shipyard-grown floors, one `orrethd` |
| Python reference and Rust plane agree byte-for-byte on canonical content | `agents/orreth-agent-sdk/tests/test_parity.py` (10/10) + the drill's parity handshake (`demo.sh drift`) |
| The full sim model is executable and conformance-held | `backend/conformance/tests/` — 291 tests green (2026-08-04) |
| Humans speak normal language to residents and get true, in-charter answers — grounded in the registry's cross-scope truth, metered under each resident's own DID, with cross-charter questions referred BY NAME under released firmware | 0046 (2026-08-04): the librarian and grace answered JB's own previously-failing questions by naming both real skills across two floors; vera, asked about the farm, answered "that's charlotte's charter — ask her directly" under the resident-voice v2 release JB cut himself (req-446) |
| The machine's driving words are readable, editable, releasable, arguable, servable, and commissionable by the humans it serves — under ONE law of change for every hand | 0045 (2026-08-03): the Canon extraction (the worker reads its own firmware from becky-signed records); JB's one-motion edit (fingertip-default v3, his authority verbatim in the body); JB's click cut a firmware release and RENAMED the machine (ff728ca5→a8c4cd1b) with the watchdog silent only on that path; a real LangGraph flow acquired the released prompt by reference and its run record named the sha; the commission birthed skill-orreth-langgraph-onboard v1 through librarian+factory+gate. Named gaps: ~~/craft lease enforcement~~ PAID 2026-08-23 — the supply door takes the SAME becky-chained lease that fuels a citizen's thoughts (POST lane; the chain verified to the pinned root, the subject must BE the claimant, a did:key registers on first sight so revocation keeps a handle while a revoked did stays dead): proven live by scout's re-join on its standing welcome then a leased acquire (leased:true), a forged-subject and a foreign-chain token each refused with the one face, and the tokenless dev-grace lane confessing loudly ("in prod this door refuses"); SDK's acquire() grew the token param and the mind presents client.token by default (SDK suite 24) · ~~the registry's two-floor sweep~~ PAID 2026-08-23 — the eco floor (e:cloud, 0038's commonality shelf) joins compose_governance wearing its floor, and the first sweep surfaced a real hidden object (distillation-dials at e:cloud); the FULL-rig sweep stays honestly named, one floor at a time with a reason · out-of-band commission rings behind a consent widening |
| The universe notices its own death without a reader and tells its human beyond the glass — under standing consent, content-minimal, record-before-wire, cooldown-bounded | 0044 (2026-08-02): the witness kill-proven Console-closed (the daemon's book spoke at 98s, later 89s); the bell's first ring in JB's real inbox (five fields only, from a one-click consent that minted a signed grant); a tampered public deed caught by the standing verify inside one window and rung; two deaths in one hour = ONE email (the second held, aged +1). The residue is NAMED: the ring waits for a resurrection (0044 §6 B3) |
| Identity survives the process; joining is governed; leases chain to a pinned root — and the roster BREATHES: leases carry terms in time, the census tells present from remembered, dormancy is a state never a deletion | 0002/0006/0012 machinery + covenant rules 1/3; the join door on the reel. The roster learns to breathe (2026-08-20, JB's find — "I like seeing it grow but have never seen it go down"): the count was a diary census that could only grow, and 73 of 92 were two-run mayflies minted fresh by every `dev.sh window` — the demo's biographer and scribe are now persistent selves (rule 1 honored in our own ritual); the beat carries `present` beside `agents`, the glass says "N present · M remembered" and folds each floor's dormant into a dormitory; becky's lease expires in ORRETH_JOIN_LEASE_DAYS (30) with re-join as renewal; the gate confesses floor capacity (ORRETH_FLOOR_CAPACITY) while the human's word still decides. NO RENAME, by law: a reused self keeps its name and diary — relabeling would counterfeit continuity |
| Refusal wears one face; the plane meters but never sees the prompt | covenant rules 4/5; uniform-refusal fixtures in the conformance suite |
| The Canon has a fingerprint on a chain; drift stages, never enacts; the revert is a sibling | 0041 live proof (req-326) + `orreth_sim/epoch.py` / `tests/test_epoch.py` (THE SILENCE suite-held) + `demo.sh drift` repeatable; universe-scope lag reconciles after the roll-up cut (`_reconcile(UNIVERSE_SCOPE)`, verified in-code 2026-07-30 — the 0041 road's "no port to stage at" gap closed with the road-step-2 work) |
| No external consequence is complete on the executor's word alone | 0042 live proof: https://demo.orreth.ai/deeds/first-deed.json — published, tampered, caught, walked back, restored; the artifact carries its own story |
| Improvement is proposed, never self-adopted; promotions keep lineage | 0031 lanes + tournament promotion receipts (req-287); orphan-lineage fix 2026-07-26 (`proposal_ref` on the card) |
| Every governed thought — and every refusal — lands on a flight recorder: latency, model, tokens, cost, taxonomy; series distill with MEASURED loss under a declared retention law, log-truth rebuildable, instrument readings labeled as such | 0043 sp1 (2026-07-30): `orreth_sim/observatory.py` + `tests/test_observatory.py` (12 tests); live: charlotte's voiced reply put a real `no-local-key` refusal AND the successful thought (301 tok · 2447 ms) on `~/.orreth/observatory/flight.jsonl` on its first flight |
| Completed work is assayed by another floor's mind — verdicts signed author ≠ executor, degradations stage as cards wearing no levers, and the examiner's own cost is metered under its own DID (pinned: never a silently cheaper judge) | 0043 sp2 (2026-07-30): `orreth_sim/vera.py` + `tests/test_vera.py` (8 tests); live: sonnet-5 (f:prod's stable) judged 13 real completed works — mean 0.14, correctly measuring the deterministic-scaffold outcomes this register already named in prose — a degradation card staged pending at the gate, and vera's parlor voice priced her own watching: 7,239 tokens, $0.02131, under her DID. 0047 sp1 (2026-08-07): the verdict is TYPED — a malformed word earns one Canon-worded re-ask (`verdict-reask`) before any void, voids are counted never silent, and `asks` rides every signed verdict (`orreth_sim/typed.py` + `tests/test_typed.py`, 13 tests; live: a 6-verdict round, zero LOST, 5 under the objective's declared rubric) |
| An Objective is READ, PLANNED, and its lacks GROWN by governed minds — reading and plan typed at every boundary (a malformed word earns one re-ask, then parks honestly), the arithmetic fallback labeled, the card the truth the human approves; the machine keeps standing duties on ONE human word until its declared ceiling rests it; the reflex arc answers events as observation, escalation, or a governed act | 0047 sp1–sp6 (2026-08-07/08): `orreth_sim/typed.py` · `standing.py` · `fingertip.author_plan`/`gap_offer` + suites 291→311, SDK 10→20; live: the studio — a jacket-wearing citizen on its own FUELED lease (the sp2 find: join leases carried no model budget, fixed) — read and planned real objectives (8 semantic legs vs 18 arithmetic on the same ask, spec hash on the card, a ghost-seat plan refused at save); 3 skills born of a NAMED gap and serving, the same ask re-read grounding its domains in them (confidence 0.45→0.7); a charter fired 2 instances on its standing word then RESTED with its renewal card; all three reflex shapes fired on real records — the bell rang, no lever moved |
| The human answers back and the answer becomes governed work: every reply and resolved objective wears a thumb; 👍 is a signed human verdict on vera's own shelf (zero cost, never anonymous); 👎's words are quoted VERBATIM, READ by the studio under a typed route contract, and land at the organ that can answer them — workshop evidence, a commission, a referral, or a repair objective wearing the human's words as its RUBRIC; feedback is a request that RESOLVES back to its author's queue; and the human's thumbs calibrate the examiner behind a declared min-n — one thumb never indicts vera | 0048 (2026-08-09, seeded→closed whole IN ONE DAY): suite 311→322; live: a 👍 faded from view and landed loud (score 1.0, the standings counted it); JB's own 👎 in the glass ("Would expect Librarian to be a basic chat resident also…") swept within a beat and staged req-543 wearing his sentence as the yardstick; the closure card "💬 your 👎 was heard — repair-staged → req-559" on the author's queue (the bell refused one-faced — the standing grant names its kinds, consent never invented); the calibration law proven AT the boundary — 4 pairs at gap 0.8625 held their tongue, the 5th spoke: ⚖ req-564 staged, cards never levers. Named gaps: ~~whole-rig feedback sweep~~ PAID 2026-08-22 — the sweep walks EVERY tended floor (universe every beat + one floor per beat round-robin, the metabolism's rhythm, cost flat): a thumb posts to the floor of the record it judges, ONE studio still reads every word, and the resolution + closure land at the feedback's OWN floor because the author's queue is there; proven live: a 👎 on an f:prod record was found by the ring within a beat, read and routed by the studio (repair-staged wearing the human's words as rubric), and the closure card came home to f:prod · craft-route evidence lands but proposals still ride the improver's own beat (0031 §4 kept) |
| The machine's sentences to humans are governed craft, tunable at gates without a code change — and a standing UAT persona walks real objectives daily as a stranger, wounding what confuses her through the thumb; the epoch watchdog knows a one-motion edit is an adoption | 0050 (2026-08-11, opened→closed whole IN ONE DAY): 13 wave-1 sentences extracted parity-proven byte-for-byte, then rewritten plain THROUGH the craft-edit door (the live calibration card shows the judged WORK, not hashes); the pass drew two honest drift accusations — `craft-edit` joined ADOPTION_KINDS with its lesson-test, accusations LEFT ON RECORD (0043's precedent); quinn's first walk: both cards judged CONFUSING (12 frictions incl. JB's own lock-4 label), routed EVIDENCED by the studio, triaged into 1 fixed defect · 3 design changes staged at JB's gate · 1 named gap. Suites 322→331 + SDK 20→22. Named gaps: ~~glass-side literals await wave 2~~ WAVE 2 LANDED 2026-08-23 (the plain-speech sweep): ten more Inbox kinds joined the shelf as craft — the passage reach-out, the legacy card, the mind/service re-pins, the entitlement ask, the death attestation, the dial turn, the examiner's degradation card, the charter rest, the smith's nudge — every one naming what it is, what each choice does, and what silence does, with design-doc citations evicted from human cards; then quinn walked the REAL rendered glass (her eye-fidelity gap paid en route) — 42 frictions, and the two views the sweep had rewritten (Inbox · Objectives) earned ZERO; her top clusters landed the same day (the "your word" idiom → plain action words, DID/pin explained in place, the repeated-tool note, the desk buttons rewritten — her stranger-eyes also caught the options desk saying "ask charles" when its specialist is chad, a real copy bug); her report (req-1165) keeps the design-level remainder on the record · refusals enter the shelf later as ONE guarded object |
| The human holds the REINS: an objective's whole life is visible on its card in plain shelf-words; the human can always STOP what the machine manages (covenant rule 11) — cancel/rest governed and recorded across all four vectors, in-flight work stopping at the next safe boundary with what-was-left-undone said; every gate takes words back, a declined gate's words becoming fuel through the thumb's loop; and the mind that reads every ask has a face, a card, and a grounded audience | 0051 (2026-08-11, opened→closed whole IN ONE DAY): the Journey strip live (the lock-4 poetry RETIRED by JB's own word); a mid-flight cancel stopped 18 fanned legs, the cancellation record under the HUMAN seat; the charter and a reflex rested — the reflex from the glass lever itself; a declined briefing's reason read TRUE by the studio, evidenced, the closure home within the minute; the studio pinned with the DID its own join proved; quinn's re-walk: original wounds HEALED («staged» became her delight), the next layer threaded into the season. Suites 334 + SDK 22. Named gaps: the studio's voiced parlor seat · ~~quinn's eye-fidelity~~ (she reads the REAL rendered glass since quinn-web, and her 2026-08-23 walk judged the wave-2 sweep: 0 frictions on the rewritten views) · ~~questions' text~~ (the plan's question tells the truth since the messaging pass) · legs' own intent text still machine-speak (the last wave-2 sliver) |
| The universe is VISIBLE measured, not claimed: an instrument room renders every panel with its tier declared (log-truth vs "instrument reading, not testimony"), the watcher's own pulse is first-class (a dark recorder turns the room dark loudly, never silently), and gate-wait ages surface how long consequence has waited for a human | 0043 sp3 (2026-07-30): the Console's 🔭 Observatory tab (`window.html`) + the worker's `/observatory` door (`compose_observatory`); proven in Chrome in both lights; the first live render surfaced a purge waiting 19d 17h at its gate — invisible before the panel existed. The standing gap register: `0043-the-observatory.md` §11 (found → written → homed) |
| A/B testing where each arm is a cryptographically named machine and the rollout is a signed constitutional act: the arm IS the floor's fingerprint with the variant as head; the split waits for a human and assigns deterministically; standings ride the log join; the conclusion is a promotion card whose adoption names the variant AND the experiment, the loser kept on the shelf | 0043 sp4 (2026-07-30): `orreth_sim/experiment.py` + `tests/test_experiment.py` (7 tests); PROVEN LIVE WHOLE on the seven rows — routing-ab (router vs hybrid): split opened by a glass click, 5 real asks wore their arms, sonnet-5 judged each under vera's declared daily ceiling, concluded at min-2, adopted by a glass click with full lineage (`adopted_from` + `derived_from` = [variant, declaration]). The watchdog then honestly accused the rollout — its vocabulary lacked the new word; JB left the accusation on record and «experiment» joined ADOPTION_KINDS, suite-held |
| The Observatory watches the WHOLE rig and itself: every tended floor round-robins through the examiner under a declared daily ceiling; the distilled pyramid survives the process; the bench is chosen, never hardwired; and the dial is a governed word that outlives the worker | 0043 sp5 (2026-07-31): whole-rig assay (19 floors, ceiling closing mid-round) · `Series.dump/load` suite-held + `summaries.json`/book-compaction live (22 sealed hours, 31-row book) · `_judge_bench` · `dial.json` — proven by kill-restart: the word held. Parked with named gates: daemon-side dead-man + out-of-band push (one future dive). G6 closed whole 2026-08-02: the "judge this by…" composer field carried a human-typed yardstick to a real `rubric_declared: true` verdict (0.12 — the declared standard measurably changed the judgment) |

| A real-world purpose runs THROUGH Orreth end to end — reports the human reads, on real market data, under governance | 0054 (2026-08-13): the CortexObserver Trading desk recreated as Orreth objectives — three desks, sixteen governed stages each, every artifact a signed record; the honesty chain proven UNSCRIPTED (the human's search ceiling flowed into the portfolio manager's capped conviction); the learning loop closed on the record ("prior report: yes · 1 lesson(s)") | the equal-or-better bar vs the reference is JB's-eye-confirmed, not yet formally scored — the memory program (0053 sp3) brings the yardstick |
| Capabilities are DECOUPLED from the Foundation — worlds atop the Machine, installed by folder, lifecycled by tile, retired to a shelf that remembers | 0055 (2026-08-13): zero by-name references in the Foundation (grep-proven) · the folder proof (a world removed is undiscovered; returned, governed) · the boot honors the shelf over the folder · the whole lifecycle walked by the HUMAN's own hands, no drift | the composer + word-kind vocabulary generalize at the first non-desk world (its named entry fee); BYOP waits on 0042 trust at cloud |
| Humans manage residents and worlds as a COCKPIT, never a document — one renderer, every room, everything ≤2 clicks and edited in place | 0056 (2026-08-17): ten resident rooms by three truths (presence · architecture · manifests) → the tabbed modal (prompts saved through the one-motion craft-edit lane, mode-gated dev/prod; schedules with the cron face, staged/rested through the resident that owns them) · the canvas (serpentine node-card DAGs, declared in manifests, live states) · command-first rooms (the ask leads; reports an index, the thirteenth canon kind) — every pass judged against the human's own reference images; his verdicts on the record ("very nice graphs" · "oh that's so much nicer") | the Objectives view demotes only by observed disuse (his lock); mind-swap picker waits on ada's gate; resident-level verdicts and the worldline walk named for THE RELATIONS' deepening |
| A human frames a piece of spacetime WITH THEIR HANDS — a window, a brush across the heat, a lane — and interrogates it in plain words: the whole universe's memory draws at the apex (lanes = the two kinds of life, sampling confessed per lane), the librarian gathers with her own seats across exactly those floors and that time (tags first, bodies only for the rows the answer reads), answers through one governed metered thought under her DID with per-record citations naming fidelity and class, the exchange lands signed in a dialog that survives reopening, and every quoted memory warms at its HOME floor (0057's law extended to the window) | mini-dive tab 3 (2026-08-19): the worker's /spacetime sweep (20,085 of 20,092 memories drawn at u:demo — the old window showed 2) + the window-ask lane; proven live in the glass: a desk-scoped ask returned the "hold" thesis graded at −2.08% alpha with 8 citations, a mis-framed window confessed "no memories under this focus" instead of inventing, a citation click opened the record body from f:charles itself, and the dialog restored its history on reopen | the focus label's memory count is the lanes' served totals, not brush-filtered; class filter chips seed the ask rather than filter the heat |
| The Stable is a REGISTRY with the knowing half of "LiteLLM executes; the Registry knows and decides": a five-eyed market (intel with provenance, never authority), effort=class from the composer to the gateway, allocation as a governed record resolved by the law of the most specific word (subject → floor → universe), blast radius on every retirement's card, a mind-swap picker in every resident's room, plain-words market questions voiced by ada under her own meter, and A/B where arms are pinned minds and one approval lands the winning allocation with its experiment as lineage | 0058 (2026-08-20, seeded→closed whole IN ONE DAY): suite 344 + SDK 23; live: 2,949 minds merged from five eyes (claude-sonnet-5 from three, access key-proven; 375 Bedrock read-only by JB's pin); one xhigh objective put three thoughts on the postgres meter at `xhigh · claude-fable-5` (the dial's freight, previously UNREAD); the librarian's voice pinned to gpt-4o-mini OVER the router's haiku preference, flight-recorded, then released; a denied retire left the stall standing (the denied-branch retired-on-any-no defect found and killed); voice-ab: 14 real asks split 4-4 on voiced draws, vera judged, haiku won 0.725–0.317, the promotion's single approval wrote the allocation ledger with `experiment: voice-ab` as lineage | the lease wound is HALF-PAID (2026-08-22 — the lease learns to renew): the fuel clause (an allowance per declared window, terms adopted from every verified token — the first-sight-only ledger that ignored vera's fresh mints is dead) is plane-enforced and held by orrethd's FIRST unit tests (6: the lump never renews, the window turns on the meter's record, quiet windows fold to one refill, replenish is the human's word, terms adopt, reconcile refunds); the drain watch (orreth_sim/fuel.py, 6 tests) cards a dry lease at the human's gate wearing its evidence — proven live on f:prod: a 450-token lump drained below the est-floor, refused uniform, was carded within a beat, APPROVED → refilled to its allowance through the plane's /model/replenish door, and a second card DECLINED held for its whole window with zero nags; and the wound closed WHOLE the same day: the rule-9 gate was held, asked, and ANSWERED — JB's explicit word added ONE optional field (budget.renew_days) to contracts/v0's Budget, and the self-renewing window ran live (a 600-token 30-second proof window drained → refused with the one face → renewed ITSELF at the turn, no human, no restart; then charlotte's real governed thought minted under the daily clause — 399,034 remaining of a 400,000 daily allowance, renews_at on the row) with parity + both suites green (conformance 357, SDK 23); the walk's own find became law: a drained subject whose renews_at is already past is FUELED (the plane renews lazily at the next debit — nothing to card); the parlor voices only keyword-missed questions (card branches answer first); AWS live adapter + per-capability conformance suites + LangGraph-node subjects are seeded parks |
| The Farm is a LIVING TOOLSHED with the registry's knowing half for tools: a seed catalog (the official MCP registry searched live, intel never authority), MCP as a whole transport through the ONE meter-as-authorization door, secrets that never enter a record (env-indirection), a content-blind warden confessing at the onboarding gate, RESTING as the human's word distinct from the wire's silence, tool allocation by the subject→floor→universe law, capability-declared tools as governed citizens wearing their source, and the keeper herself as the playground — testing tools beside the human and answering for her shed under her own meter | 0059 (2026-08-21, seeded→closed whole IN ONE DAY): suite 350; live: tavily-mcp planted by env name (five tools enumerated through the resolved link, probation earned, one governed tavily_search in 2.4s counted on the SAME daily ceiling as the direct path, the credential found in ZERO of ten ledgers, zero records, zero archived requests); rest staged wearing its blast radius → the door refused with the one face for free → resume re-earned serving; a dirty proof-endpoint drew all three warden warnings and the deny left nothing standing; the rig restart itself surfaced two REAL rug-pull catches (deepwiki + huggingface manifests changed while away); charlotte, asked for eyes on scientific papers, inventoried HER OWN SHED first and confessed the catalog's edge — voiced, metered, cited | NAMED: stdio/package seeds need a body — container deploys are allen's walk; ~~policy-as-craft~~ PAID 2026-08-23: the warden's CHECKS stay firmware (vigil never stops looking) but her eight WORDS — the confessions and the credential-pattern list her nose runs on — live on the 0050 speech shelf, planted as v1 assets at boot and tunable through the one-motion craft-edit door (suite 359; proven live: a dirty plant confessed from the shelf, «warden-dead-probe» was rewritten through the door — "nobody answered at this address — you can still plant it, but it will sit dark until the tool wakes and earns its probation" — and the very next dirty plant spoke the human's words, the old version behind it with lineage); ~~recent-callers~~ PAID 2026-08-22: the meter-log door stands (GET /farm/callers, folded plane-side from the meter's own entries — per service, each distinct caller DID with calls and last-seen, suite-held in orrethd's farm tests) and every rest/decom/reapprove card's blast radius now names WHO LEANED ("leaned on by …" with real caller DIDs, organ names where known) beside the allocations — and the proof walk FOUND AND KILLED the farm's own denied-branch defect (0058's mind-branch lesson, farm-shaped: a declined REST was decommissioning the service; now a denial of rest/resume/decom/reapprove/allocate moves NOTHING — "your word was about the verb, never the service" — proven live: a declined rest left com.microsoft/learn serving); JB's three-restart find KILLED (2026-08-22): the capability-tools intake asked at u:demo while the real tool SERVED on f:charles, and its idempotency died with the process (in-memory roster empty at boot) — V2 asks at the capability's OWN floor (the genesis names it), reads standing from the floor's durable ledger and «settled» from the queue's surviving words whatever their fate, and retries from the beat for worlds that wake after the worker — "the gate never re-asks what a human settled" is now true across restarts (proven: a fresh boot logged «already stands … at u:demo/e:desk/f:charles», zero ghost cards); charlotte-crafted sample args PAID 2026-08-23 (playground v2: «✎ propose a test» reads the tool's PINNED schema and fills every required field by its declared type, defaults honored, optional fields counted honestly — deterministic and free, no thought spent proposing what the schema already says; the proposal survives the tick like the playground's answer); node-level fidelity carries JB's lock and comes only as a question; publishing Orreth's own capability-born MCPs to the registry is a future 0042-shaped dive |
| The kernel's RESIDENTS MAINTAIN THEMSELVES — standing supply lines for their own domains (subscriptions via the serials desk, farm-fetched, quarantined on arrival, own-metered under declared ceilings), the study SPEAKABLE from the listen kit, and the residents build for each other: allen grows container bodies as 0042 deeds so charlotte can plant stdio tools she once refused | 0062 (2026-08-26/27, seeded by JB's own phrase, closed whole era 0.62): allen's three CDK/AWS lines opened on JB's word and delivered within the hour; asked "what did you learn this week?" he answered VOICED with that week's real news, quarantine confessed in his own words; the container walk proven twice — server-everything (15 tools) then pulse-fetch chosen LIVE from the catalog, each: body-needed card → one word → five-record deed on allen's worldline (epoch cited, recipe-hash key) → bridged body unless-stopped → charlotte's probe pinned the LIVE wire → probation → a governed tools/call on the meter; the one-Inbox cure deepened 2026-08-27 (JB felt the absence, the walk found the wound): the universe root wears every floor's waiting cards AND living purposes within a second of a fresh load — the boot fetch had died silently on a constant declared below it, leaving 15s of false calm; the registry's PYTHON half opened the same day on JB's word — pypi bodies ride astral's official uv image with a uvx bridge (mcp-proxy pinned to mcp==1.9.4, an upstream drift confessed in the recipe; the /mcp/ slash law), proven whole: aws-docs — awslabs' own PyPI package named directly, the catalog intel never authority — walked deed → plant → SERVING and answered a governed search_documentation on the meter — and the SAME DAY the circle closed: allen's docs study line was allocated to that door (subject-level allocation, 0059 §2.7's most-specific-word), the serials desk learned to ride an allocated tool through the governed wire with args from the pinned schema, and issue 2 delivered three real docs.aws.amazon.com claims sourced to the tool's own DID — the architect studies AWS through the door he grew; en route the desk's memory-only wait counter was cured (a restarting rig re-zeroed every weekly line's wait — the last note's real age now seeds it) | HONEST: the smoke proofs ride estate-apply (the dev estate IS the laptop) — a dedicated «body-run» effect class awaits its release ceremony; ~~allen speaks at u:demo~~ PAID 2026-08-27: a resident with a home floor now VOICES AT HOME — the authorize and meter land on its own plane (proven: allen's study answer moved f:allen's meter 0→$0.0014 while u:demo's stood still); the hull was never unfueled — no one had asked it to authorize; Azure/Google are declared growth, not claims |
| A STRANGER'S first contact is designed, not endured: the canon vocabulary (the register is load-bearing — saddled ≠ configured) is TAUGHT, never renamed — the machine's own dictionary lives as ~35 gloss-* shelf-craft definitions (plain, slotless, citation-free, suite-held; one-motion editable at the gates; served live-head-first through /sentences), every canon word wraps as a dotted door in the glass (first occurrence per render, text nodes only — 0052's law extended from record-refs to vocabulary), a five-stop welcome band greets the first visit (dismissible, the header's ? brings it back), and the rail sheds its e:/f: prefixes for display (the colored dots carry kind) | 0060 (2026-08-25, from the first-contact design talk — JB's four locks all on recommended paths, quinn's walks req-1193/1218/1241 the evidence trail): suite 361; the closing walk (req-1283) proved THE RAIL DIET WHOLE — the e:/f: complaint vanished from every room for the first time in five walks — and the band + dotted doors render live (screenshot-proven); her density verdict on the band was paid at close (five stops became five lines) | HONEST: quinn's eye is view-level — she can SEE the dictionary doors but not OPEN them, so "her lists stop naming undefined canon words" cannot fully fire until the click-flow walk (quinn v2, named in web.py since birth); the vocabulary floor itself (agent, gate, memories as words) remains by DESIGN — the dictionary is the standing, tunable answer |
| A human can OPEN the work from any card, and no card leaves them wondering: record refs are written WHOLE at authorship (a cut id is a door that can never open — the glass shortens for the eye, never the data), the record-reader walks every floor through one door and names the record's home, a true whole-rig miss explains itself (distilled or purged — the departure is on the record), a question that outlives its purpose withdraws itself with the whole story, and the plan's one question tells the truth: the work is already running, only an optional delivery step waits, silence skips it on a named clock | the messaging pass (2026-08-23, JB's brief: "we can't leave humans wondering what's going on"): the reflex card that baked `ref[:22]…` into prose — the id destroyed at authorship — now carries the whole ref in text AND field, with the same sweep over the feedback loop's cards and the version walk; GET :4562/record hunts every tended floor (proven: the thumbed f:prod record opened from u:demo naming its home; a legacy cut ref drew the honest confession); the stale delivery question JB found withdrew ITSELF within a beat of the reaper landing ("nothing to answer anymore — the plan behind it … already finished"), and a fresh objective walked the whole arc live: asked with the honest face → expired on its 3-minute clock → withdrew at assembly naming its parent | glass-side reader fallback needs the rebake to serve; the reflex's own event sentence is DECLARED data (its reflex record) — editable through its own door, not this pass |
| The metabolism is UNIVERSAL and forgetting is MEASURED — every discovered floor breathes round-robin on a floor-local ground (tags first, bodies only for the lineage the loss walks), usage is evidence wherever it happens (ask hits, opened reports, quoted answers — warmed at the record's HOME), and every distillation lands a report with its loss in bits | 0057 (2026-08-17): 8 floors breathed live incl. the 6.5k-record universe ground ≤2.7s/breath and a 5-day-old capability floor honestly due-less; a month-overdue record warmed by ONE touch survived the next breath (187 distilled around it, 1 kept warm); the Brain pull renders the breaths per floor from the machine's own state; EN-ROUTE FIX: the wire beat had never landed a loss report since wire-honesty week (9 distillations, 0 reports — a treeless projection crashed the loss walker after every real distillation) | cross-floor lineage reads as the projection's confessed horizon (stub/missing bits), not resolved across floors; distillation summaries remain the deterministic v0 scaffold, not distilled PROSE; the machine-task CRUD surface — tabled by JB's word to the UI season — was BUILT when that season closed (2026-08-22): THE MACHINE'S TASKS in the Brain pull — every no-resident standing word in one table (posture · cadence · next due) with the reins (run-now, a cadence editor whose edit is a RE-STAGED sibling through the same gate, rest/schedule), the machine's fixed rhythms confessed beside it (the metabolism's breath, the examiner's dial) with their tuning pointed at their own homes; proven by the human path in the glass: ✎ cadence → staged → the card in the Inbox wearing its terms → one approval → the new sibling became the head, the old word on the record |
| The machine's operating VALUES are governed craft: declared as firmware (name, type, bounds, blast, why), valued on the shelf in their own dial-* drawer (L1's separation — never interleaved with the words that steer minds), turned by a human through the one-motion door, taking hold LIVE within a confessed horizon — bounds enforced read-side with genesis fallback, loudly, never silence | 0063 sp1 (2026-08-28): three dials planted as becky-signed genesis (search-daily, assay-ceiling, kindrank — two spend ceilings and the Inbox's triage ORDER, proving a dial is any tunable VALUE, not just a number); the first governed turn (6→2) took hold in 45 seconds on the /sentences door; a 999 head outside [0..100] landed and was refused ALOUD («genesis serves») while the rig kept breathing; the ordering dial put questions first then reverted, the sibling chain keeping every step; suite 365 with a rule-7 parity test pinning the glass's genesis literal to the registry's. ~~Named remainder: the DOOR is untyped until sp2~~ PAID the same day (sp2): the door refuses BEFORE landing — a 999 turn through the REAL glass editor was refused with the full teaching (bounds · unit · governs · blast) and NOTHING landed (v4 stood); a clean turn lands CANONICAL ("2" becomes 2), every landed sibling carries its teachings (no dial record is a bare number with amnesia), the glass's craft room shows the dials drawer with its teaching strip, and the prod razor learned dials are PURPOSE; suite 368. THE LADDER (sp3, same day): declarations wear a HOME, a floor's own shelf outranks the universe by the 0059 resolution (proven live: f:prod's metabolism-batch 50 breathed while the universe served 200), a universe dial REFUSES a floor's door with the lesson, and the improver's self-tuning cap is the HUMAN'S WORD (L3 paid — the code's own 5 became dial-improver-cycle-cap, threaded to both nudge sites; at the cap nothing proposes, suite-held); per-resident rungs wait for a resident-homed enforcement site, named not faked; suite 370. THE ROOMS (sp4, the day's fourth): the settings inventory lives in the Brain pull — every dial's LIVE value · bounds · home · governs · ✎, drawn from the shelf never from paper — and a dial's mention is a door IN PLACE (the Observatory's «ceiling you set ✎ TURN IT», the Farm's search-guard band); goDial() lands any mention in the craft room's dials drawer with the record open — ≤2 clicks, the Commander law held, proven in the glass. THE BODY (sp5, the day's fifth): the Atlas's fourth lens 🔩 — the rig's physical anatomy as the schematic's basement, four families each beside its governing ledger, glow MEASURED (docker + the ledgers; the three red floors were the down-ledger's own words — the grave, charlene, chad), the rig-down word rendered as the operator's word, every box a door (a hull steps onto its floor, proven by click); the morning's «where's the view that correlates infra to Orreth objects?» answered in the glass. THE SWEEP'S FIRST WAVE (sp6-w1, the day's sixth): the CADENCE FAMILY — twelve organ rhythms left the code in one motion (passage·embed·mirror·monitor·improver·epoch·lag-window·metabolism·assay·verify·cal·brain-census), env demoted to genesis, each with bounds and blast; ONE drawer read serves all seventeen dials (tags first, bodies for heads only, pre-warmed); proven live — metabolism-every turned 900→300 and the Brain's own rhythm row said so within the horizon, canon restored; ATTEST_EVERY honors JB's standing lock, SILENCE_UNIT stays a dev rig-dial; the remaining waves are NAMED in the dive doc, not silently pending. WAVE 2 (2026-08-29): THE MONEY FAMILY — the study lines' defaults (cadence-beats, budget-calls) and the fan-out pair (budget-tokens, min-share) became dials; proven end to end: the dial turned 4→2 and a freshly approved study line's minted terms READ «2 call(s) per delivery» — inheritance live — then the proof-line was cancelled through the parlor's own unsubscribe (no proof-planted spend left standing) and canon restored; vera.EST_TOKENS confessed sim-only (skipped, not swept), the chassis's agent-side max_tokens a named horizon. WAVE 3 (same day): THE BELL PAIR — bell-cooldown (floor 300: the inbox's peace has a firmware floor) + bell-gate-age; the singleton hears the word LIVE (cooldown refreshes on every service — proven: the Observatory's bell view read 900 within the horizon, no restart); a below-floor turn refused with the teaching; gate-age deliberately never lowered on a rig with old cards standing (the blast line names why); the Rust witness floor stays firmware — the clamp pattern's home. WAVE 4 (same day): LEASES AND FUEL — join-lease days/tokens, the renew window (0 = the lump posture, the vera-419 family), floor-capacity as the LADDER'S SECOND CITIZEN (a floor declares its own room; the gate confession informs, the human's word decides), and fuel-est-floor threaded through the sim's posture; THE TWO-TRUTHS TRAP CURED — the code's 50000 and the rig's 400000 were never the same number, and the shelf now shows the standing truth (planted genesis reads 400000); −1 refused with the teaching; 28 dials. WAVE 5 (same day): THE DESK'S WINDOWS AND THE JUDGMENT BARS — seven more (success-floor, studio-dark, schedule default, the calibration pair, the assay pair) and the registry learned FLOAT because the bars are fractions; the sim's signatures had anticipated their dials (calibration/degradations were already threaded — the worker now passes the human's words); proven: the first float turn 0.4→0.25 served in the horizon, 1.5 refused with the teaching; NAMED not swept: the desk's holding period is CAPABILITY territory by the razor (its numbers ride the 0055 craft lane, never the kernel drawer), rivals/infotheory sim-only, the constitutional trio keeps its locks; 35 dials. WAVE 6 + THE CLOSE (2026-08-29, era 0.63): recall-days — how far the universe reaches into its own past — paid sp0's promise with the recursion law on its face (the dial drawer's own read keeps the firmware year: reading the dials never depends on a dial); the attestation cooling-off stays FIRMWARE by L2 (a death's abort margin is not a prod lever); **THE DIVE CLOSED WHOLE at THIRTY-SIX dials** — int, float, and an ordering, one drawer read, two ladder citizens, the improver inside the human's word, six waves proven by turn·refusal·restore, the settings room + teaching strips + the Atlas's body lens making the machine legible from value to hull; the unswept PURPOSE rows are ordinary follow-ups on standing machinery — a declaration plus a swap, never new machinery |

| The machine has a PUBLIC BOOK and a stranger can build on it: documentation at its own address teaching use AND construction in plain-first language (canon as labels, the tested dictionary as the bridge), with every tutorial page WALKED before written — and the docs are the decoupling's own forcing function (a page that cannot be written honestly names a seam; a cut seam ships as the page's proof) | 0064 (2026-08-31, seeded→chartered→13 pages→closed in ONE DAY): docs.orreth.ai live (Starlight, public repo iotlodge/orreth-docs, CC BY + MIT); orreth.ai + www 301 to the book (path-preserving, deep-link-proven); THREE SEAMS CUT with stranger-shaped proofs — ghcr.io/iotlodge/orrethd publicly pulled · `pip install orreth-agent` (0.1.0, Apache-2.0, 18 public names, live-index install proven) · the manifest contract taught (13 panel kinds) with the SDK's mirrored vocabulary; the first-world example (2 profiles + compose + a mint script) booted u:first beside the rig with join/cascade/console proven; the hello capability walked install→shelf-remembers→true-retirement; a LangGraph agent from OUTSIDE the repo refused at the gate, admitted on one human word, and REMEMBERED ACROSS LIVES (4→5 records, same DID); the dictionary grew the five words the book exposed (36→41, suite-held); quinn walked the LIVE book with real eyes (req-1686 — her true strikes paid within the hour, her mute on record) | honest remainders: the clean-machine quickstart walk · formal manifest JSON-Schema · S7 (the sim/wire fork) parked with its name · quinn's book-walk cadence |

## 2. Reference-proven or intentionally partial (from the poster's §12, now ours)

| Boundary | Where it stands | The gate that moves it |
|---|---|---|
| A human runs the machine from ONE seat — everything waiting decidable in one place, every mention a door | 0052 (2026-08-12): mechanics proven live — master-detail inbox, in-pane decisions with the human's words, linkified doors, the record-reader — but the vision owner's lived verdict stayed "rather unusable": legible furniture atop an undifferentiated card soup | 0053: the two books worn on every card + a real Chronicle purpose beneath; the seat re-walked then |
| Several cognition paths are deterministic scaffolds or simulated judges; the plane correctly authorizes and meters while execution stays cognition-side | intentional v0 posture; the LIVE judge exists (mentee haiku · judge sonnet, another floor's mind) but falls back to the honestly-labeled sim judge when ground is thin | widen live-judge coverage; keep the fallback labeled |
| The wire objective fingertip proves choreography + receipts more strongly than open-ended autonomous work | true; the walks are real but the flows are composed | harder, less-scripted Objectives through the fingertip, receipts unchanged |
| Request-status transitions are persisted but unsigned — signer-registry work unfinished | named in the worker itself ("by the human seat (v0 — unsigned until 0012's registry)") | 0012 registry build |
| Embedder and projections degrade honestly when unavailable; production-scale rebuild unproven | pgvector projection live (0022 Phase 2); purge reaches the projection | a rebuild drill: drop a projection, rebuild from the log, diff |
| The dev rig proves the institution on one machine; federation, hosted custody, DR, multi-tenant need their own operational evidence | honest; nothing pretends otherwise | 0013 handshake + custodian tier + an operational runbook with receipts |
| Write-time re-hash: orreth-node trusts the author-signed id at ingest, never recomputes hash(body) | OPEN rule-9 question, parked in `../decisions/` | revisit BEFORE 0013 or any external ingestion door |

## 3. Designed or deliberately parked (each behind a named gate)

| Parked | Design home | Gate |
|---|---|---|
| Hosted Custodian tier; blindness/BYOK/split-key proof on production infrastructure | 0011/0013 | the hosting decision + a partner's real custody need |
| Federation and transport beyond parent-HTTP dev topology | 0013 | rule-9 re-hash decision first, then the handshake dive |
| Class-allocated storage provisioned from the Canon's retention/RTO/RPO map | 0039 (the Chronicle & the Canon) + allen's charter | the storage-map spoonful, allen provisions |
| Live-judge mentor/mentee graduation across real model tiers at sustained scale | 0040-era graduation pipeline (first graduation is a receipt) | sustained-scale runs with cost receipts |
| The Faculty package + Agent Lab | 0040, triaged and reserved | **JB's stamp: built only when Orreth is "stamped ready to test it"** |
| Dynamic "missing capability → gather → crystallize → canary → serve" as a general product capability | 0028/0031/0032 proved the organs separately; **0047 sp5 (2026-08-07) RAN THE WHOLE LOOP live on an objective** — the gap named by the reading, the offer checked, three skills commissioned/welcomed/serving, the replan grounded in them | the remaining generalization: the same loop on an arbitrary human-authored FLOW — 0040's Lab, at its stamp |
| Long-duration human-gate ergonomics, operational quorum, memory-metabolism tuning at scale | scattered (0030 gates · 0033 dials) | the playing phase, with real universes |

## 4. Shelf note

The poster itself stays verbatim in `../outside/` (nothing there is canon).
This register is the canon-side absorption of its §12 — the outside mind's
inventory, adopted as our own proof obligations. Sections 1–3 above supersede
memory of "what's honest" scattered across dive docs: when in doubt, a claim
not on this page with evidence named is a claim we do not make.
