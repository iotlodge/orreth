# 0043 — The Observatory

<!-- PROVENANCE: Fable 5 (claude-fable-5) — designed with JB 2026-07-30; four locks
     landed same session (vera · two-tier law · the name · rows-first-rig-whole). -->

*The universe learns to measure what it actually does.*

## 0. The law

Every observability product is a third person: an external witness watching a
system through exported, unsigned trace blobs, judging them in a foreign court
— someone else's SaaS, someone else's retention, someone else's judges. Orreth
refuses the sidecar, because it holds an advantage no sidecar can copy: **the
trace already exists, and it is signed.** An Objective decomposing into
Intentions, each Intention meeting Observations, each Thought metered and
chained by `derived_from` — that is a governance-grade distributed trace that
the industry approximates with JSON and hope.

So the constitution extends, rather than bends:

> **Observability is a projection over the signed log — never a second truth
> (rule 7), never a federation. Evaluation is a seat that is not the actor
> (rule 2). Depth costs money and says so (0019). And the instruments render
> in the same glass as everything they watch.**

Other stacks film the robot. Here, the world remembers — and the Observatory
reads.

## 1. What already stands (the audit)

The rivals' feature lists, walked against the constitution:

| They sell | We hold |
|---|---|
| Traces (LangSmith runs, DeepEval component spans) | O·I·O·T + the walk of the work — signed, budgeted, walkable |
| Cost/token tracking | the universal meter (0019), subtree-aggregated, honest zeros |
| RAG metrics (RAGAS: faithfulness, context precision/recall) | coverage · fidelity labels · explicit remainder · epistemic status · **measured distillation loss with sealed samples** (0033) |
| Experiments & comparisons | the tournament and the standings (0005 · 0038) |
| LLM-as-judge | the judge from another floor's stable — independence *enforced*, never hoped |
| Config attribution for A/B | **the Epoch**: every outcome cites a cryptographically named machine (0041) |
| — nothing — | the witnessed Deed: ground truth beyond the executor's word (0042) |
| Annotation queues | the gate pattern; human verdicts as signed records |

**The honest gap is five things**: a time-series organ (events exist, series
don't) · span timing on flow nodes · a *standing* quality loop (the tournament
is on-demand; nothing continuously assays live work — 0042's deferred
"standing verify beat" wearing its true face) · the instrument glass · the
generalized experiment.

## 2. The organ — vera, the astronomer

**LOCKED (JB, 2026-07-30): the Observatory gets its own resident.**

vera — for Vera Rubin, who measured what galaxies actually did against what
their visible matter claimed, and found the invisible mass. She keeps the
Observatory. Her laws:

1. **She is the inverse of vigil.** vigil is content-*blind* detection; vera
   is content-*aware* assay. The separation is what keeps vigil trustworthy
   and vera useful; the two never merge.
2. **She never grades her own floor's homework.** Her judges are always
   another floor's minds (the graduation pipeline's independence, made a
   standing law). Her verdicts land as signed records under the judges'
   authorship, scribe-witnessed — author ≠ agent, always.
3. **Detection wears no levers** (0041 §4, inherited whole). A finding —
   a degrading score, a drifting distribution, a losing experiment arm —
   stages at a human gate. vera measures; humans move.
4. **Her own cost is her first exhibit.** Every assay she commissions rides
   the meter under her DID; the Observatory's price is always one of its own
   instruments.

## 3. The storage law — two tiers

**LOCKED (JB, 2026-07-30): two-tier.**

- **Assay records** — judge verdicts, standings, experiment results, human
  gradings — are **signed Chronicle records**. They are institutional
  experience; they feed the 0039 evidence→Canon loop directly; they are
  never pruned, only distilled.
- **Instrument series** — per-call latencies, counters, rates, distributions
  — are a **projection with declared retention**: rebuildable from the log
  wherever they claim log-truth, and labeled *"instrument reading, not
  testimony"* where they are performance-derived. And the metabolism applies
  to its own telemetry: raw series distill to hourly, hourly to daily, with
  measured loss, like every other memory. **Even the monitoring ages
  honestly.**

## 4. The taps

Rule 5 makes this dive cheap, because the choke points already exist:

- **The model plane gateway** (the stable's LiteLLM-direct door): every
  resident and workforce thought already passes through it to be metered.
  The meter extends into a flight recorder — latency, model id, tokens
  in/out, refusal taxonomy, per governed thought. An organ that thinks
  off-meter is drift; therefore coverage is total *by construction*.
- **The farm gateway** (charlotte): every tool call — volume, latency, error
  rate, per service worldline. Rug-pull correlation falls out for free.
- **The fingertip**: flow nodes gain span timing (start/end/status riding
  the choreography records that already exist).
- **The doors**: gate-wait ages — how long consequence waited for a human is
  a first-class metric, and almost nobody in the industry measures it.
- **The beats**: epoch, metabolism, serials — they already emit; vera reads.

## 5. The dial — glance · watch · assay

The brain's gain dial, reused for depth-of-instrumentation (JB's toggle):

- **glance** — free: series and counters only.
- **watch** — spans, distributions, percentiles.
- **assay** — the Examiner beat turns on: vera samples completed work on a
  cadence and commissions independent judges, on a budget the meter shows to
  the decimal. This is "online evaluation" told honestly: quality costs
  money, and the dial says how much.

## 6. The assay loop

On the beat (assay dial only): sample recent completed Intentions/Objectives
→ route each to a judge in another floor's stable with the rubric its
Objective declared → verdicts land as signed records → standings aggregate →
degradations stage findings at the gate. Human gradings enter the same shelf
by the same shape (a card, a verdict, a signature) — LangSmith's annotation
queue collapses into a gate we already have. The Mirror remains the
conversation-assessor; vera assays *work*; neither grades itself.

## 7. The experiment — A/B where arms are machines

The crown jewel, and mostly assembly:

1. Two Canon variants planted as versioned assets (grace's lanes, 0031).
2. **Each arm is an Epoch** — a signed, named machine (0041). No metadata
   tagging, no "did we record the config?" — the arm *is* the fingerprint.
3. The dispatcher splits traffic by a declared, human-approved policy.
4. Standings accumulate per-arm (0038's machinery, generalized beyond rows).
5. The conclusion is not a dashboard. It is a **promotion card at the
   human's gate**, the losing standard versioned behind the winner, the
   adoption naming its evidence (the 0038 orphan fix's lineage law).

*A/B testing where each arm is a cryptographically named machine and the
rollout is a signed constitutional act.* That is the sentence we market.

## 8. The glass — the instrument room

Hand-rolled canvas in the brain/orrery bloodline — no Grafana aesthetic, no
chart library unless we hit a wall — living in **both lights** (on the
Daylight Glass, the panels should read like a naturalist's field notebook;
that is a design goal, not an accident). Instruments: sparklines, horizon
strips, small-multiple distributions, cost flows, standings ladders. SVG/PNG
export like the estate DAG. Every panel declares which tier feeds it
(log-truth vs instrument reading).

Panels, loaded generously (JB: load the views):

- **The Objective ledger** — opened/completed/parked/failed · time-to-close ·
  retry loops · escalation-ladder events · parked→librarian→retry outcomes.
- **Cognition** — cost/latency/tokens per seat·floor·mind · refusal rates ·
  doubt-escalations · **context-ridden-down size** (the blast-radius number
  nobody else has) · judge-score distributions.
- **Memory & metabolism** — records/day by kind · distillation runs and
  measured loss curves · quarantine promotions/discredits · purge reach ·
  recall walks.
- **The RAG observatory** (JB's named requirement) — seven rows' standings
  over time · per-question round wins · coverage/fidelity/remainder
  distributions · promotion history annotated with epochs.
- **Governance** — epochs cut per floor · drift raised/resolved · **silence
  streaks** · uniform-refusal pressure (probe weather) · gate-wait ages ·
  attestation convergence lag.
- **Farm & stable** — tool call latency/error by worldline · price drift ·
  canary outcomes · deal utilization.
- **Continuity** — heartbeat gaps · lag windows · testament states.

## 9. The locks (all landed 2026-07-30)

1. **vera, a new resident** — the tenth organ; vigil stays content-blind.
2. **Two-tier storage law** — assay records signed; series a distilling
   projection.
3. **The name: 0043 The Observatory.**
4. **First canvas: the seven rows — and THE WHOLE RIG IS NOT OPTIONAL.**
   JB's words. Rig-wide coverage is a committed spoonful; the timing is the
   builder's call, the destination is law.

## 10. The spoonfuls

| # | Spoonful | Nature |
|---|---|---|
| 1 | **The flight recorder** — plane + farm taps, fingertip spans, gate ages; the series projection and its retention/distillation law · **LANDED 2026-07-30**: `orreth_sim/observatory.py` (readings · four taps · Series · FlightRecorder) + taps in `model_plane`/`farm`/`fingertip`/`hitl` + the worker's `FlightBook` wire twin (ledger-seeded, aged honestly) · 265/265 · live: the first voiced reply put a real `no-local-key` refusal and its successful thought on the book | the senses |
| 2 | **vera and the assay loop** — the resident, the Examiner beat, judge independence, human verdicts, the dial · **LANDED 2026-07-30**: `orreth_sim/vera.py` (the four laws structural: bench never on the work's floor · refusal over self-grading · degradation CARDS · cost metered under her DID, PINNED — no silently cheaper judge) + `observatory.percentiles` (the watch depth) + the wire (vera seeded/pinned at the universe floor — the improver's precedent, builder's call; `assay_beat` dial-gated, default glance; judge = sonnet-5 from f:prod's stable signing via its floor seat; her parlor card) · 274/274 · proven live on the rig | the judgment |
| 3 | **The instrument room** — the Observatory glass, both lights, first canvas the seven rows (the RAG observatory lands here) · **LANDED 2026-07-30**: the 🔭 Observatory tab — six hand-rolled panels (cognition/flight-recorder · vera's standings w/ score strip · gate-wait ages · the seven rows · farm & stable · governance/epochs), every panel wearing its TIER chip, supplied by ONE composed payload from the worker's own door (:4562, CORS-open, the plane untouched — rule 9); the recorder's pulse breathes in the header (G1) and an unreachable door turns the room dark LOUDLY; per-canvas PNG on double-click (G9); proven in Chrome in BOTH lights — the first render surfaced a purge that had waited 19 days at its gate, unseen until this panel existed | the seeing |
| 4 | **The experiment** — A/B arms as epochs, dispatcher split, standings, the promotion gate; proven on the rows | the argument |
| 5 | **The whole rig** *(committed, per JB's lock)* — every floor wearing instruments, the universe-level rollup, the Observatory watching the Observatory's own cost | the universe examines itself whole |

## 11. The gaps the build found — a standing audit (JB's directive, 2026-07-30)

*"Really don't want gaps in monitoring and visibility."* This section is the
Observatory watching itself: every gap the build exposes gets a row HERE, in
the design, with its closing spoonful named — never just a session note. A
gap without a named home is a gap we are choosing to keep, and we don't.

| # | Gap | Why it matters | Home |
|---|---|---|---|
| G1 | **The watcher's own pulse.** The recorder lives inside the worker; a dead worker takes the Observatory dark *silently* — the one failure a monitoring organ must never have | who watches the watcher | ✓ **visible half CLOSED sp3**: the pulse breathes in the room's header (brass · amber >45s · dark >150s), and an unreachable door turns the WHOLE room into "THE WATCHER HAS NO PULSE", loudly. The daemon-side dead-man (a floor noticing worker silence) rides sp5 |
| G2 | **Durable summaries.** Wire hourly/daily summaries rebuild from the 7-day book; instrument history older than the book's horizon leaves the wire projection entirely (declared, but a cliff) | long-baseline curves (distillation loss over months) need the pyramid to persist | sp5 — persist distilled tiers beside the book, same declared retention |
| G3 | **Wire taps beyond the plane.** Farm calls, gate-wait ages, and flow spans are sim-proven but were wire-dark after sp1/sp2 | the design promised four senses; the wire had one | ✓ **gate-waits + farm/stable CLOSED sp3** (the observatory door composes them from books that already exist — and the first render showed a purge that had waited 19 days, unseen); wire flow spans ride sp5 |
| G4 | **The dial is invisible and env-only.** No one can SEE what depth the universe is paying for, and changing it is an operator act, not a governed one | depth costs money and says so — but only if it can be seen | ✓ **visibility CLOSED sp3** (dial + price posture in the room's header); the governed dial-change (a staged request, a human's word) rides sp4 with the experiment's controls |
| G5 | **The assay budget is implicit** (sample cap per beat, no declared ceiling) | §5 promises "a budget the meter shows to the decimal" | ✓ **shown sp3** (sample-per-beat + assay cadence in the header; vera's cumulative spend on her panel); a declared per-day ceiling lands with sp4 |
| G6 | **Wire rubrics are always the default.** Objectives on the wire declare no rubric; every verdict says `rubric_declared: false` honestly | assays should judge against what the objective ASKED | sp4 — the experiment needs declared rubrics anyway; the objective request gains an optional rubric field |
| G7 | **The wire bench is fixed** (f:prod's stable, the graduation precedent) — correct but unchosen | judge diversity once more floors hold minds | sp5 — bench selection across serving stables, still never the work's floor |
| G8 | **Alerting beyond the glass.** A degradation card staged at an unopened Console is a tree falling in an empty forest; gate-expiry-is-denial bounds the harm but does not notify | the last mile of visibility | PARKED, named: the Console is the surface today; out-of-band push (the parlor's channels, a webhook door) is its own small dive after 0043 — it touches identity and consent, not just plumbing |
| G9 | **Panel export.** §8 promises SVG/PNG export like the estate DAG | receipts leave the glass | ✓ **per-instrument PNG landed sp3** (double-click any canvas keeps it); full-view SVG rides sp5 polish |

The law of this table: **found → written → homed**. When a row closes, it
moves to the spoonful table's landing notes; when a new gap surfaces — in
build, in review, in JB's use — it enters here first.

---

*Forty-two dives taught the universe to remember, to argue, to govern its own
change, and to let no consequence pass unwitnessed. This one gives it what
every institution needs and no agent stack has: an astronomer — who measures
what the universe actually does, prices her own curiosity, and stages what
she finds where a human holds the key.* 🥂
